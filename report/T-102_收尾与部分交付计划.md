# T-102 收尾与部分交付计划

> 更新时间：2026-09-15 20:20 CST（UTC+08:00）。原18:40计划已执行；结果5/5完成。

## 执行结果

- 共同候选五个原方法、安装态无候选五个原方法、部署前后各六项设备边界均通过；原失败保留。
- 五个独立OFF/ON门禁、30个新进程性能臂全部验签；1～4改善、5回退。3/4原社区随机数值oracle缺失、5数学展开，均明确部分对齐。
- 两个experimental文件已按授权备份并窄部署，本地产品提交`3a0b817`；原T-106 codegen未变，不修改上游或默认禁用配置。
- 结果与逐pattern解释见[交付索引](../results/current/T-102/README.md)，总体54/69与未完成边界见[部分交付说明](部分交付说明_20260915.md)。
- 本次不推送产品仓、不发起PR或宣称社区CI通过。下方“本轮确认”为18:40执行前记录，后续部署以本节及原件为准。

## 交付门槛

T-102 包含 SFDP pattern 1～5。逐例完成原合同、最小修复、安装态新进程复验、合法同后端 OFF/ON 及六臂性能处置；候选通过不计正式完成。部分交付只列已完成单元，其余问题保留原失败与下一步。

## 本轮执行前确认（18:40历史）

- 后端固定 `triton_experimental`，冻结 PyTorch `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`，不修改上游。
- 物理 NPU 5 空闲后逐例运行；NPU 1 的既有 VLLM 进程不触碰。功能持共享性能锁和单卡独占锁，性能全局独占串行。
- 当前 codegen 已包含 T-106 修复，SHA256 `86f310ce99960eb12ead8b67db7264c068a56f9ecb5e0e7a43a6e48cbcad5a57`，不得覆盖回旧版本。
- pattern 1、2 隔离注册候选已重新执行，父记录分别为 `native-20260915T183109-2nzpe1d2`、`native-20260915T183632-qnfqmo3f`。尚未安装。

## 必须保留的数值范围

```python
# PyTorch test/inductor/test_fused_attention.py:137、140
if not has_dropout or override_check_equal:
    self.assertEqual(result1, result2, atol=atol, rtol=rtol)
# training 分支仍执行 backward；has_dropout 时也不做原梯度比较。
```

pattern 3、4 的随机 dropout 原合同只支持结构命中与执行结论；不能把零 Tensor 比较写成精度 PASS。独立性能输入采用已审查的极低非零 dropout 方法，保留本编号训练注册，不以相邻推理编号代替。

## 顺序

1. 原失败 FX/IR/生成代码与实际 NPU 分解逐例复核；候选保留原 search/replace/extra_check，不绕过禁用配置。
2. 候选与边界通过后按已授权范围备份、窄部署；新进程不加载候选重跑，绑定实际安装源码哈希。
3. 独立功能 OFF/ON 通过后才签性能门禁，六臂计时不与功能任务并行。
4. 归档功能/性能讲解、适配与修复报告、补丁和未覆盖边界，更新正式统计。
5. 准备部分交付总入口，明确已部署、候选、回退、不可独立归因、延期及历史再认证；不宣称所有 188 个候选或所有 FX pass 已完成。
