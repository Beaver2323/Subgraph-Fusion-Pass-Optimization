# 四个 PR 组合精度验证

更新时间：2026-09-20（UTC+08:00）。模型流水已于 19:14 结束，98 项全部出结果。


**本轮 98 项中，90 项精度通过；官网 9 月 18 日有成功结果的 61 个对应项，本轮全部精度通过。**

| 结果 | 数量 | 模型与模式 |
| --- | ---: | --- |
| 精度通过 | 90 | 详见逐项结果 |
| 仅性能通过 | 3 | maml_omniglot 动态开/关、Background_Matting 动态关 |
| 精度失败 | 2 | hf_DistilBert、resnet50_quantized_qat 动态关 |
| 超时 | 2 | timm_efficientnet 动态开/关 |
| 运行报错 | 1 | Background_Matting 动态开 |

这 8 个非精度通过项在官网 9.18 对应模式均为 missing，保留本地异常，不计为通过，也不作为相对当日看板的新增退化。resnet50_quantized_qat 动态开启已精度通过。

该结论是四个 PR 合在一起的模型验证，不能写成四个 PR 各自完成全量测试。流水之后发现并修复了 BF16 value=0.1 问题，**本轮模型数据不包含这次新修复**；修复后的 addcdiv 专项测试 17/17 通过，完整模型尚未重跑。

[逐模型原始状态](pr-validation-20260920/models/status.tsv) · [官网逐项对照](pr-validation-20260920/models/website-comparison.json) · [后续专项结果](四个PR专项测试结果_20260920.md)

## 目的与范围

将 !46464、!46465、!46466、!46468 的当前代码共同应用到一份 master，先验证具体修复，再跑一次模型集。以精度正确为主要验收标准，允许共享 NPU，不做性能验收。新失败只针对相应模型进一步拆分补丁定位，不重复执行四轮完整模型测试。

| 内容 | 提交 |
| --- | --- |
| master | `a5fa5f6ea8cff0095b7a74d00c7110c181207a40` |
| !46464 Attention 训练匹配 | `6bb5dbe3789b27c8640e2c77762b3f3eb0ff263a` |
| !46465 设备解析 | `10dfa1448e20ebb414e80a774205f83ade7fe7f9` |
| !46466 E8M0 编码 | `a524b537b856c9deddd6ece98989487468125e65` |
| !46468 addcdiv 舍入 | `cc0ded239cb1bad9981cf62f48adea79a5c79402` |
| 组合测试提交 | `459cc35748db200f61f7769e8b25cf247f6254cb` |

独立代码目录：`/mnt/share/z50063656/AscendForge/src/combined-prs-20260920`。未改动远端 PR。

注册入口的冲突保留 Attention 和 E8M0 两个调用；lowering 的冲突保留主线 softmax 函数和 PR addcdiv/div 函数，AST 对照确认函数体一致。语法检查及 `git diff --check` 通过。

## 环境

- 从 `/home/z50063656/tmp` 启动所有测试。
- CANN：`/home/crm/cann/cann-9.2.0`。
- 后端：导入前选择 `triton_experimental`。
- NPU：0、1、2、4、5、6、7，共享使用，排除 3 号卡。
- PyTorch 源码版本：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。
- 环境：`/mnt/share/z50063656/AscendForge/envs/torchbench-models`。备份已有后端后，同步组合提交的 `triton_experimental` Python 文件；原生库仍为 `0095e028…` 所构建版本。这是 Python 后端组合验证，不是最新 master 完整 wheel 构建验证。
- profiler 关闭；编译缓存、调试输出和 CANN 日志存储在本轮共享目录内。Triton 编译临时文件使用本机 `/home/z50063656/tmp/combined-four-prs-cann92-20260920-compiler-tmp`。

## 执行安排

1. Attention 注册 UT、addcdiv FP16 精度 UT、CooR 原例、E8M0 指数范围检查。
2. Attention 1～5 号社区原方法。保留原断言；3/4 号原方法没有随机数值对照，不能据其通过推断所有 dropout 数值场景通过。
3. 专项检查通过后，自动进入 49 个模型 × 动态形状关闭/开启，共 98 项训练 FP32 精度测试。原脚本默认仅计时的模型本轮也尝试精度检查，不以计时成功替代精度通过。
4. 超时、OOM、跳过、未执行及没有最终结果的项目单列，均不计入精度通过。

网站 `triton-ex` 用于后续功能状态对照；共享卡的时间数据仅供排查，不用于宣称性能收益或回退。组合通过也不等于四个 PR 分别完成独立全量验证。

## 结果位置

本轮目录：`/mnt/share/z50063656/AscendForge/runs/combined-four-prs-cann92-20260920`。

- `progress.json`：执行阶段及更新时间。
- `identity.json`、`pr-inputs/`、`input-commits.json`：本轮代码、环境与文件哈希。
- `specialized/*.log`、`specialized/results.json`：专项检查输出和退出状态。
- `pipeline.log`、`status.tsv`、`results/`、`logs/`：模型执行记录与逐项结果。
- `runtime-backup/`：同步组合版本前的后端备份。

统计依据是本轮 status.tsv 与逐模型 CSV。流水末尾的自动审核脚本因缺少 result_summary.csv 退出失败，不能宣称自动审核通过；本文根据已保存的逐项结果核对计数，官网对照也单独保留。

## 专项检查结果（2026-09-20 13:24，UTC+08:00）

修正临时目录后，本轮注册 UT 5/5、addcdiv FP16 UT 1/1、CooR 原例 1/1、Attention 1～5 原方法 5/5 均通过，无跳过；E8M0 `exponent-sweep` 数值及目标改写检查通过。对应 9 个执行进程全部退出 0，框架用例数与 E8M0 脚本检查分别统计。

模型预检通过，13:24 启动 7 个 worker，19:14 全部结束。最终结果见本文开头。这里的运行前专项检查与后续新增的各 PR 专项测试分别记录。

## 启动过程中的环境修正

首轮将 `TMPDIR` 也放在共享 NFS，CooR、addcdiv、E8M0 和 Attention 5 在 Triton 编译清理临时目录时出现 `Errno 39: Directory not empty`。目录内可见 `.nfs*` 文件，尚未执行到数值比较；Attention 1～4 与注册 UT 已通过。首轮输出保存在 `specialized-nfs-tmp-attempt/` 和 `launcher-nfs-tmp-attempt.log`。

仅将临时目录切换到本机有空间的 home 分区，组合代码和数值断言保持不变，随后复跑专项。共享目录继续用于大体积缓存和结果。
