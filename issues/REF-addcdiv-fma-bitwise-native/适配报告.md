# REF-addcdiv-fma-bitwise-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-fuse-addcdiv-to-fma`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：CUDA 测试包装去除与 NPU 入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

`CommonTemplate.test_addcdiv_fma_bitwise_equal` 依赖 CUDA `GPUTests` 生成，NPU 没有原生 test body。
该负向/精度合同要求 bitwise equality，不能放宽为普通 tolerance。

## 2. 最小适配

```python
# issues/REF-addcdiv-fma-bitwise-native/npu_adapter.py:65-81
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestCase):
    device = "npu"
    # 绑定 CommonTemplate 原方法；仅关闭 CUDA-only runtime 调试断言
```

只替换设备、测试类生成和 CUDA-only dtype/shape static-assert 插桩；原输入、bitwise 断言和 FMA
产品 guard 均保留。

## 3. 必要调用链

源码重建链：`NpuCase → CommonTemplate original method → torch.compile → addcdiv-to-FMA pattern
→ triton_experimental codegen → bitwise assertion`。

## 4. 结果与边界

适配入口执行成功并保留 bitwise 合同；单元经后续条件修复及性能评估正式为 `PERF_NEUTRAL`。
完整行为与候选边界见同单元 [codegen 复现报告](../REF-addcdiv-fma-codegen-native/复现报告.md)。
