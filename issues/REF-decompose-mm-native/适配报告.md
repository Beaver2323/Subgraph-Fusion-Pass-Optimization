# REF-decompose-mm-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-077｜`AU-decompose-mem-bound-mm-decompose-mm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：六变体社区阈值镜像与修复复验入口；`product_gate_bypassed=false`

## 1. 原生阻断

社区 `test_decompose_mm` 与 `test_decompose_mm_mixed_precision` 在 NPU 原生文件执行中 skip。adapter
保留 fp32/mixed 各自正例、K 阈值负例和 M 阈值负例，共六变体。

## 2. 最小适配

```python
# issues/REF-decompose-mm-native/npu_adapter.py:95-114
with torch._inductor.config.patch(post_grad_fusion_options=options):
    left = torch.randn(m, k, device="npu", dtype=torch.float32)
    right = torch.randn(k, n, device="npu", dtype=torch.float32)
    compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

适配镜像社区 shape/dtype/autocast/threshold，记录 FX 与 lowering；默认不绕过 device guard。独立候选
模式只用于验证已登记 small-MM lowering 修复，不能混作安装态 baseline。

## 3. 必要调用链

源码重建链：`adapter.main → six variants → post_grad decompose handler → aten.mm or
unsqueeze/mul/sum → NPU small-MM lowering → generated kernel → correctness/counter assertion`。

## 4. 结果与边界

安装态正例因 guard 未分解；候选启用分解后暴露错误 pointwise lowering，正式 comparison 保留
`NPU_REGRESSION`、repair=`verified`。候选 `dfbcc25` 的首个分歧、代码框及 6/6 回归见
[根因分析](根因分析.md)和[修复验证报告](修复验证报告.md)，尚未合入产品。
