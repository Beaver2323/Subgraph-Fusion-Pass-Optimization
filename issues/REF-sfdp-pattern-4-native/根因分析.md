# Pattern 4：乘 scale 的 dropout 训练注册兼容缺口

> 更新时间：2026-09-15 19:07 CST（UTC+08:00）。隔离候选通过，尚未部署。

## 测试目的与根因类型

`softmax(QKᵀ * 0.4) → dropout(p=0.2) → V` 的 joint-graph 训练改写，覆盖普通/checkpoint。根因为图匹配行为不一致：CUDA 预生成训练图不包含实际 NPU dropout 与梯度分解形态。

## 判定依据与调用栈

```text
# 冻结 PyTorch test/inductor/test_fused_attention.py:355、121
_test_sdpa_rewriter_4 → _check_common → torch.compile → joint-graph matcher
 → self.assertGreaterEqual(counters['inductor']['fuse_attention'], 1)
 → AssertionError: 0 not greater than or equal to 1
```

原安装态在训练首次断言失败，前面的推理由 pattern 2 接替；不能把邻接推理计数当成本编号生效。详见[原失败与实际生成代码](安装态基线复核.md)。

```python
# evidence/t102-native-f7l8j1r5/adapter/registration-candidate/target-_sfdp_pattern_4_training.py
# NPU 实际 trace 使用以下算子族，完整参数与前后图见链接原件。
torch.ops.npu._npu_dropout.default
torch.ops.npu.npu_dropout_backward.default
torch.ops.aten.matmul_backward.default
# softmax backward 保留 mul/sum/mul/sub，不恢复 CUDA 预生成图的 FMA。
```

## NPU 能力匹配评估

候选完整原方法 1/1、零 skip，2 次本编号训练改写。实际 6 份生成图包含 NPU attention-v3 前向和 grad-v3 反向，无 CPU 替代。社区 dropout 例不比较 Tensor 输出/梯度，故这里是执行/结构通过，不是精度通过。

## 修改文件清单、关键代码与修复理由

目前没有安装态改动。候选 `runners/attention_registration_candidate.py` 仅在 NPU 本编号 training 注册时按实际分解追踪原 search；保留原 replacement、extra_check、scalar_workaround 和去重条件。

```python
# runners/attention_registration_candidate.py:23，关键流程节选
pat, gm = pm.gen_pattern_and_search_gm(search_fn, inputs, trace_fn, scalar_workaround)
pm.register_replacement(**kwargs, pattern_name=name, search_fn_pattern=pat)
```

不修改原 dropout 概率，不调整数值容差，不取消 softmax 精度保护。NPU 特有分解应在后端注册入口处理，不写冻结 PyTorch 源码，不修改 Triton 编译器。

## 初步验证日志

[完整命令、stdout/stderr、退出码](candidate_runs/native-20260915T185642-j_c1kiuy/)；返回码 0，`Ran 1 test` / `OK`。[实际 FX、IR、生成代码](evidence/t102-native-f7l8j1r5/adapter/)原样归档。

## 硬红线自检结果

| 检查项 | 结果 | 依据 |
|---|---|---|
| CPU 兜底 | PASS | 候选不含迁移；实际前向/反向调用均为 NPU |
| disabled_testcases 新增 | PASS | 无禁用项或 skip 新增 |
| 无无关变更 | PASS | 隔离的本编号训练注册，安装包未改 |
| 根因-修复一致性 | PASS | 按真实分解重建注册，不改变算子数值语义 |

## 下游

继续产品化共同入口、负例和布局边界、安装态新进程复验，再验证同后端 OFF/ON。低非零 dropout 微基准只证明其输入域，不补造原 p=0.2 随机性精度结论。
