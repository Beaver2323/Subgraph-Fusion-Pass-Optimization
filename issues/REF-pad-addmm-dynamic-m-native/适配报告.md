# REF-pad-addmm-dynamic-m-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-addmm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：dynamic-M 测试入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

`PadMMTest.test_pad_addmm_dyn_m` 受 GPU-only harness 阻断。社区合同包含 bias `[128,40]`、mat1
`[128,33]`、mat2 `[33,40]` 及动态 M，不能缩成静态或无 bias 测例。

## 2. 最小适配

```python
# issues/REF-pad-addmm-dynamic-m-native/npu_adapter.py:41-53
@upstream.inductor_config.patch(
    max_autotune=True, max_autotune_gemm_backends="TRITON,ATEN", force_shape_pad=True
)
def test_dynamic_m(self):
    compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

只适配设备/backend/入口和 ATEN fallback，完整 bias、动态 shape 和数值合同保留，产品 pad gate 未绕过。

## 3. 必要调用链

源码重建链：`adapter test → dynamic torch.compile(addmm) → pad_addmm registration/gate
→ NPU lowering → first/replay correctness`。

## 4. 结果与边界

adapter 1/1 通过且 dynamic-M 正确；padding rewrite 关闭，正式 verdict 为
`EXPECTED_PRODUCT_DIVERGENCE`。简要结果和后续保留要求见[复现报告](复现报告.md)。
