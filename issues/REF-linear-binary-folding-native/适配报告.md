# T-100 Linear binary folding 最小适配

> 更新时间：2026-09-14 20:49 CST（UTC+08:00）；原社区完整方法已通过。

## 1. 原生阻断

原生 `FreezingGpuTests` 只在HAS_GPU为真时定义，当前NPU入口为_FailedTest，原件在native_runs。
目标是冻结Linear参数后把合法add/sub/mul/div常量折入weight/bias；非法逐batch广播不折叠。

```python
# PyTorch test/inductor/test_binary_folding.py:227，原方法保留
@inductor_config.patch({'enable_linear_binary_folding': True})
def test_linear_binary_folding(self):
    # 原参数乘积、2D/3D、正负counter和atol=5e-5/rtol=5e-6保持不变。
    ...
# tracker runners/module_folding_community_npu.py
with mock.patch.object(inductor_utils,'HAS_GPU',True), mock.patch.object(inductor_utils,'GPU_TYPE','cuda'):
    spec.loader.exec_module(community)
# 继承原复制类，只设置device='npu'，不用重新编写功能图。
```

## 2. 最小适配

本目录 `npu_adapter.py` 调用共用适配器。仅原类发现与设备路由变更，
`product_gate_bypassed=false`，实际后端为 `triton_experimental`。

## 3. 必要调用链

原unittest → torch.compile → freezing → binary_folding.folded_op → NPU lowering/codegen → 原数值与counter断言。
原setUpClass中的freezing/discard/implicit_fallbacks配置保留。对handler只读捕获前后FX。
只改harness设备，不改源码、shape、dtype、断言或产品gate；NPU始终triton_experimental。

## 4. 结果与边界

通过run_prepared_npu_case.py的T-100原case加--adapter执行；结果见adapter_runs及后续证据目录。
本轮1/1通过、0skip，176次输出比较、160正例折叠和16负例不折叠均通过；
详细命令、工件及原社区bias变量未实际生效的覆盖边界见[复现报告](复现报告.md)。未签性能gate，未部署产品修复。
