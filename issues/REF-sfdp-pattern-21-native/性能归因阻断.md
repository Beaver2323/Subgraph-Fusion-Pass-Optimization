# Pattern 21：OFF被邻接22接替，不能归因本编号性能

> 更新时间：2026-09-15 17:21 CST（UTC+08:00）。最终处置：功能通过、性能无法独立归因；用户已接受结项。ON未运行，未计时。

## 最终处置与统计

用户已[明确确认](最终处置确认.md)接受当前合同的归因限制。正式状态为
`PERF_NOT_INDEPENDENTLY_ATTRIBUTABLE`，不写成中性、回退、收益或默认关闭免测。
本号计入功能/comparison完成及最终处置数，但性能实测数、收益数、默认关闭免测数均增加0。
T-106因此为4/4已处置：22/23/24共3项实测，21号1项归因受限；原件退出码1不变。
现有输入限制保留，不据此声称所有可能输入都无法独立归因；不制造新的ON/OFF门禁。
可移植原件绑定见[处置记录](../../results/current/T-106/functional/pattern-21.json)。

以下为作出处置所依据的实测与历史分析，原失败未被改写。

9月15日后续实测：部署22号select-load修复后，在空闲NPU6新进程执行同一21号性能输入，
仍在数值比较之后触发 `RuntimeError: OFF仍命中fuse_attention`，退出码1。
[新运行完整文本原件](evidence/functional-20260915T112553+0800/)包含执行命令、错误栈、
精确 `_sfdp_pattern_22_half_inference` 改写前后FX和最终IR/output_code。
这不是本轮引入的新数值失败；原9月14日失败原件继续保留。

```python
# 冻结PyTorch8e86e0a，torch/_inductor/fx_passes/fuse_attention.py:628、654
# 两个search主体相同；22额外返回K/V。语义节选：
def pattern21(q, k, v, mask):
    return attention(q, k, v, mask)
def pattern22(q, k, v, mask):
    return attention(q, k, v, mask), permuted_k, permuted_v
```

原子模式匹配不简单等于Python返回值列表全等；本次21图仍由22的可匹配输出子图替代，
这是具名观察器的实际证据，不仅是源码相似度推断。
不能为了“完成”删除邻接22、换别的dtype/shape后未经独立审查计时，或写成显式disable免测。
当前只证明已选输入的独立收益不可归因，不宣称数学上所有可能输入都不存在独立对照。

## 9月14日原始结论（历史保留）

原社区NPU方法通过（两组输出比较和本编号改写）；新注册half微图删除21号注册后，
仍被`_sfdp_pattern_22_half_inference`匹配并真实改写，因此执行器拒绝计时。
[OFF原件与精确目标观察](evidence/functional-20260914T232759+0800/)已保存；数值检查先通过，再在归因门禁失败。

```text
删除 _sfdp_pattern_21*，保留整轮joint及其他注册
  → 邻接 _sfdp_pattern_22_half_inference 接替并改图
  → NPU eager数值对照通过
  → RuntimeError: OFF仍命中fuse_attention
```

这不是数值错误，也不是产品明确disable。不能为了制造ON/OFF差距扩大OFF去删除22，
然后仍称21单pattern收益；那会改变因果问题为一个pattern组的收益。
后续先审查原社区输入能否构成不被邻接接替的合法本编号对照；若不存在，则如实记目标级不可独立归因，
而不是宣称加速、中性或默认关闭免测。当时正式comparison/性能计数不增加；本次最终处置以上方用户确认及独立统计为准。
