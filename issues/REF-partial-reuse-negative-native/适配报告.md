# REF-partial-reuse-negative-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-reuse-partial`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：small-static/dynamic 负例参数适配；`product_gate_bypassed=false`

## 1. 原生阻断

`test_unsuccessful_partial_reuse_case0` 同样受 `HAS_GPU` 启动门阻断；decorator 捕获了 CUDA device
参数。合同要求 small-static 和 dynamic 两组均 counter=0。

## 2. 最小适配

```python
# issues/REF-partial-reuse-negative-native/npu_adapter.py:47-59
method = inspect.unwrap(upstream.TestPatternMatcher.test_unsuccessful_partial_reuse_case0)
def test_small_static(self): return method(self, ((4, 8), "npu"))
def test_dynamic(self): return method(self, ("dynamic", "npu"))
```

只把 decorator 捕获的 `cuda` 等价改成 `npu`；shape、dynamic 标记、原方法和 counter=0 判据保持。

## 3. 必要调用链

源码重建链：`two adapter methods → unwrapped upstream negative method → torch.compile
→ partial-reuse guards reject → original reduction → correctness/counter=0`。

## 4. 结果与边界

2/2 负例通过，证明修复没有扩大匹配域；单元正式为 `PERF_MIXED`。原生/适配 run root 和 FX/IR
说明见[复现报告](复现报告.md)。
