# REF-partial-reuse-negative-native NPU 复现报告

> 更新时间：2026-09-06 07:55 CST（UTC+08:00）
> 阶段结论：`PASS`（两个社区 guard 分支均不命中）
> NPU 后端：`triton_experimental`

## 基本信息

- **上游测试**：
  `test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_unsuccessful_partial_reuse`。
- **冻结 PyTorch**：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`；目标测试文件
  SHA256 为 `6a8a98d8f957f78ec99801b1cfd9d104bafb00cef9c2fbe59f3879c6340ea6fe`。
- **GPU reference**：紧凑 handoff 中 2/2 分支通过，FX before/after 签名相同。
- **测试职责**：验证不应发生 partial→full 复用的边界，不参与性能收益计算。

| 分支 | 输入 | 不命中原因 | 期望 |
|---|---|---|---|
| small-static | FP32 `[4,8]` | `numel=32 < 4096` 收益阈值 | counter=0 |
| dynamic | FP32 `[2048,64]`，dim0 dynamic | 当前合同不对 symbolic numel 做静态收益判断 | counter=0 |

## 原生入口与最小适配

原生入口因上游 `HAS_GPU` 只覆盖 CUDA/XPU/MTIA，退出 0 但执行 0 个测试；stdout
为空且没有编译工件，因此记录为入口 blocker，不记 PASS。

原生日志：
`/home/z50063656/tmp/t078-npu-results/REF-partial-reuse-negative-native/direct-eqk67u1x/`。

最小适配：

1. 导入前选择 `triton_experimental`；
2. `GPU_TYPE=npu`；
3. 将 decorator 在 case0 捕获的 `cuda` device 参数等价替换为 `npu`；
4. 显式调用原方法的两组参数，保留 `[4,8]`、`[2048,64]`、dynamic 标记、
   4096 阈值和所有原断言。

## 复现命令

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
export ASCEND_RT_VISIBLE_DEVICES=0

python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/issues/REF-partial-reuse-negative-native/run_native.py
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/issues/REF-partial-reuse-negative-native/run_native.py --adapter
```

## 执行结果

```text
Ran 2 tests in 97.096s
OK
small-static: partial_reduction_reuse actual=0, expected=0
dynamic: partial_reduction_reuse actual=0, expected=0
```

有效日志：
`/home/z50063656/tmp/t078-npu-results/REF-partial-reuse-negative-native/adapter-my8wfs22/`。

两个分支均完成 compiled/eager 输出比较，没有 failure、error、skip 或 CPU fallback。

### FX guard 证据

small-static 与 dynamic 的 `fx_graph_readable.py` / `fx_graph_transformed.py` 均保持：

```python
amax = torch.ops.aten.amax.default(arg0_1, [0], True)
amax_1 = torch.ops.aten.amax.default(arg0_1)
```

即 full reduction 仍读取原输入，没有错误改成 `amax(amax(x, dim=0))`。dynamic 分支
的 transformed FX 输入维度保留 `f32[s77,64]`，确认没有丢失 symbolic shape。

## 结论

NPU `triton_experimental` 正确遵守两个上游 guard。结合相邻正例修复后的 3/3
counter=1 和 NaN 近邻 PASS，`AU-post-grad-reuse-partial` 的功能门禁已闭环：

- 正例 3/3 命中且数值正确；
- 负例 2/2 不命中且数值正确；
- min reduction helper 编译与 NaN 传播正确；
- 未修改冻结 PyTorch、收益阈值或测试合同。

性能仍需目标级 fresh-process OFF/ON worker，不能用本轮功能耗时推断收益。

---

### Handoff

- **阶段结论**：PASS
- **社区负例**：2/2 PASS，counter 均为 0，0 skip
- **下一步动作**：该 acceptance unit 转入性能 worker 准备；T-078 功能流水线继续下一个 addmm 单元
- **传递工件**：`复现报告.md`、`run_native.py`、`npu_adapter.py`、两组 FX/IR/output_code
- **阻塞条件**：GPU raw-text FX 正文待补，但不阻塞当前 guard 结论
