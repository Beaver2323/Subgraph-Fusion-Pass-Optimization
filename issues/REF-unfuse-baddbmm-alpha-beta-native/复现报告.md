# REF-unfuse-baddbmm-alpha-beta-native NPU 复现报告

> 更新时间：2026-09-06 08:22 CST（UTC+08:00）
> 阶段结论：`PASS_WITH_DEVICE_ENTRY_ADAPTER`
> NPU 后端：`triton_experimental`

## 测试合同

上游方法：
`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_unfuse_broadcast_bias_baddbmm_alpha_beta`；
冻结 PyTorch commit `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`，测试文件 SHA256
为 `6a8a98d8f957f78ec99801b1cfd9d104bafb00cef9c2fbe59f3879c6340ea6fe`。

社区测例固定输入为 bias `[4,1,8]`、mat1 `[4,6,5]`、mat2 `[4,5,8]`，并执行：

```python
# test/inductor/test_pattern_matcher.py:1565
torch.nn.functional.relu(
    torch.ops.aten.baddbmm(inp, a, b, alpha=0.8, beta=0.2)
)
```

它要求 compiled/eager 数值一致，同时要求生成代码中没有 baddbmm、仍有 bmm。意图是验证
拆分后非默认标量没有丢失：数学表达式仍为
`relu(0.8 * bmm(a, b) + 0.2 * inp)`。

## 原生优先与最小适配

- 原生入口：`direct-snj6kvnx/`，退出码 0，但因社区 `HAS_GPU` 只识别 CUDA/XPU/MTIA，
  实际执行 0 tests；该结果不是 PASS。
- 最小适配：仅把 `GPU_TYPE` 改为 `npu` 并显式实例化原测试方法；没有修改函数体、shape、
  alpha/beta、数值断言或 FileCheck。
- 有效工件：
  `/home/z50063656/tmp/t078-npu-results/REF-unfuse-baddbmm-alpha-beta-native/adapter-u79l6n53/`。

```text
Ran 1 test in 28.361s
OK
```

`adapter_result.json` 记录 `tests_run=1`、`skipped=0`、`community_assertions_passed=true`。

## FX 与生成代码核验

transformed FX 明确显示目标改写已生效：

```python
# .../fx_graph_transformed.py:4
bmm_default = torch.ops.aten.bmm.default(arg0_1, arg1_1)
mul_tensor = torch.ops.aten.mul.Tensor(bmm_default, 0.8)
mul_tensor_1 = torch.ops.aten.mul.Tensor(arg2_1, 0.2)
# 随后执行 add 与 relu
```

原始 NPU `output_code.py` 调用 `extern_kernels.bmm(...)`，Triton pointwise 内同时保留
`0.8` 和 `0.2` 常量；未出现 direct baddbmm 调用。由此可区分“测例返回码通过”与
“目标 rewrite 真正发生”。

## 结论与边界

- 功能、数值、结构和非默认 alpha/beta 语义均通过；0 skip、无 CPU fallback。
- 本 case 不需要 torch_npu 产品源码修复，也不需要 codegen 字符串规范化适配。
- GPU 紧凑 handoff 已证明同一社区 case 有效；GPU FX/日志正文仍待 raw-text handoff，
  因此当前可做 hash/状态对照，不能声称完成逐行 GPU/NPU 图对照。
- 本轮功能耗时不是性能数据。性能必须使用独立 fresh-process、目标级 OFF/ON worker。

## 执行命令

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
export ASCEND_RT_VISIBLE_DEVICES=0
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/issues/REF-unfuse-baddbmm-alpha-beta-native/run_native.py --adapter
```

---

### Handoff

- **阶段结论**：PASS_WITH_DEVICE_ENTRY_ADAPTER
- **目标行为**：`baddbmm(alpha=0.8,beta=0.2) → bmm × 0.8 + bias × 0.2`，再与 ReLU 融合
- **产品修复**：不需要
- **下一步动作**：该 acceptance unit 进入目标级 OFF/ON 性能测量
- **阻塞条件**：GPU raw-text 图正文尚未回传，但不阻塞 NPU 功能和独立性能阶段
