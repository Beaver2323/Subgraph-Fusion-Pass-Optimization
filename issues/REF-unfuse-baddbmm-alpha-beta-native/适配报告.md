# REF-unfuse-baddbmm-alpha-beta-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-unfuse-bias-baddbmm-to-pointwise`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：非默认 alpha/beta 原方法实例化；`product_gate_bypassed=false`

## 1. 原生阻断

`test_unfuse_broadcast_bias_baddbmm_alpha_beta` 因 `HAS_GPU` 不识别 NPU 而 0 tests。它固定
`alpha=0.8, beta=0.2`，不可用默认标量 case 替代。

## 2. 最小适配

```python
# issues/REF-unfuse-baddbmm-alpha-beta-native/npu_adapter.py:42-48
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
suite = unittest.TestSuite([NpuCase(METHOD)])
```

只替换设备/入口，alpha、beta、broadcast shape、数值和 codegen FileCheck 不改，产品 gate 未绕过。

## 3. 必要调用链

源码重建链：`original alpha/beta method → torch.compile → baddbmm unfuse scalar guard
→ preserved or pointwise path → correctness/FileCheck`。

## 4. 结果与边界

adapter 1/1 通过并证明非默认标量分支按合同处置；单元最终按各 shape/标量性能为 `PERF_MIXED`。
完整 FX/IR/output_code 与命令见[复现报告](复现报告.md)。
