# REF-unfuse-addmm-half-enabled-native NPU 免测说明

> 更新时间：2026-09-06 08:13 CST（UTC+08:00）
> NPU 处置：`EXEMPT_EXPLICIT_DISABLE`
> 适用后端：`triton_experimental`

## 处置结论

不在 NPU 上执行
`TestPatternMatcher.test_unfuse_bias_addmm_half_dtypes_when_flag_disabled`，也不为其构造
性能 ON 路径。

该社区方法的唯一作用是把
`keep_addmm_fused_for_half_dtypes` 从默认 `true` 显式改为 `false`，从而允许 half
addmm 被拆分。冻结上游 handler 对非 XPU half dtype 的默认行为是直接返回，以避免 mm
中间结果额外截断引起精度回退。这构成明确的产品/后端精度保护，而不是普通的未知能力 guard。

按照项目长期约束：

- 不临时关闭明确 disable/gate 来制造 ON；
- 不对该人为 ON 路径测性能；
- 已完成的 GPU 社区结果保留，不删除、不改写为 NPU 结论；
- NPU 只实测默认 gate，结果见相邻
  `REF-unfuse-addmm-half-preserve-native/复现报告.md`：bf16/fp16 2/2 均保留
  `extern_kernels.addmm`。

## GPU 已有证据的边界

GPU handoff 中该 case 已执行并通过，可证明冻结 PyTorch 在 GPU 上的配置开关行为；它不证明
NPU 关闭保护后的精度安全，也不进入 NPU 支持率或性能收益分母。

---

### Handoff

- **阶段结论**：NPU 明确关闭免测
- **动态执行**：0（有意不执行，不是 skip 冒充 PASS）
- **GPU 证据**：保留为上游行为证据
- **性能处置**：EXEMPT
- **下一步动作**：更新 T-078 manifest/reference/performance plan 的 NPU 处置字段
