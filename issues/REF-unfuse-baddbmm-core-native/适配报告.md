# REF-unfuse-baddbmm-core-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-unfuse-bias-baddbmm-to-pointwise`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：原方法实例化及可审计 codegen 符号归一化；`product_gate_bypassed=false`

## 1. 原生阻断

`test_unfuse_broadcast_bias_baddbmm` 受 `HAS_GPU` 门阻断。NPU 生成代码可能以
`torch.ops.aten.*.default` 表示与社区 `extern_kernels.*` 等价的直接调用，导致字符串 FileCheck
先于语义判断失败，详见[复现报告](复现报告.md)。

## 2. 最小适配

```python
# issues/REF-unfuse-baddbmm-core-native/npu_adapter.py:62-76
def run(self, code):
    normalized = code.replace(
        "torch.ops.aten.baddbmm.default(", "extern_kernels.baddbmm("
    ).replace("torch.ops.aten.bmm.default(", "extern_kernels.bmm(")
    return self._inner.run(normalized)
```

原生成代码原样保存；归一化只作用于 FileCheck 输入副本，并记录两种 spelling。原 shape、broadcast、
数值和检查 needle 不改，产品 pass/gate 不绕过。

## 3. 必要调用链

源码重建链：`NpuCase original method → torch.compile → baddbmm unfuse handler
→ triton_experimental lowering/codegen → read-only spelling normalization → original FileCheck`。

## 4. 结果与边界

适配消除表示差异后社区核心合同通过；产品条件修复与性能已验证，单元正式为 `PERF_MIXED`。
归一化不是产品代码修复，也不能掩盖真实 op 路径；原始 codegen 观察保存在 adapter result 中。
