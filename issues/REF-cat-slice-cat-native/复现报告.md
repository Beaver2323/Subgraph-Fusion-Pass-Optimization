# REF-cat-slice-cat-native NPU 复现报告

> 更新时间：2026-09-07 18:45 CST（UTC+08:00）
> 状态：功能 PASS，性能 `PERF_IMPROVED`
> 后端：`triton_experimental`

## 1. 社区合同

社区入口：`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_cat_slice_cat_cuda`，冻结
PyTorch `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。合法 `size=19` 且 first width=32 时折叠
两次 cat；first width=8 的越界分支和 `end=-1` 分支进入等价 fallback。原方法对三路分别校验数值、
matcher count 与 nodes。

```python
# PyTorch: test/inductor/test_pattern_matcher.py:737
cat_1 = torch.ops.aten.cat.default([a, b], 1)
slice_2 = torch.ops.aten.slice.Tensor(cat_1, 1, 0, 19)
return torch.ops.aten.cat.default([cat_1, slice_2], 1)
```

## 2. 原生复现

从 `/home/z50063656/tmp` 执行原模块，因 `HAS_GPU=false` 未调用 `run_tests()`，0 tests；因此为
`NO_TESTS` 而非 PASS。

## 3. 最小适配与调用链

适配说明见 [`适配报告.md`](适配报告.md)。源码链：社区方法 → `torch.compile` → post-grad matcher →
`post_grad.py::cat_slice_cat` → lowering/codegen → 原 `common()` 数值与 counter 断言。

## 4. 动态结果

1/1 tests PASS、0 failure/error/skip。原方法的三组 counter 断言均为 matcher=1、nodes=3，合法折叠与
两个 fallback 的数值均通过。目标级 6 臂性能结果：NPU Event p50/p99 改善 18.41%/16.88%，host
p50/p99 改善 20.83%/17.24%，结论 `PERF_IMPROVED`，无需产品修复。

功能工件：`/home/z50063656/tmp/t079-npu-results/REF-cat-slice-cat-native/adapter-l_h0o2kz/`；性能工件：
`/home/z50063656/tmp/t079-performance-results/cat-slice-cat-performance-20260907T101924+0800/`。
