# REF-cat-slice-cat-native NPU 最小适配报告

> 更新时间：2026-09-07 18:45 CST（UTC+08:00）
> 任务/单元：T-079 / `AU-post-grad-cat-slice-cat`
> 后端：`triton_experimental`

## 1. 原生阻断

原模块 `__main__` 受 `HAS_GPU` 启动门阻断，返回 0 但执行 0 tests；不是产品 pass 失败。

## 2. 最小适配

```python
# issues/REF-cat-slice-cat-native/npu_adapter.py
runner = Path(__file__).parents[2] / "runners" / "t079_npu_case.py"
sys.argv = [str(runner), "--case-id", "REF-cat-slice-cat-native", *sys.argv[1:]]

# runners/t079_npu_case.py
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
```

`adapter_deviation`：只注入设备、显式实例化原方法并关闭测试诊断插桩。`preserved_contract`：三条
shape/end 分支、原 `common()`、数值和 matcher count/nodes 不变。`product_gate_bypassed=false`。

## 3. 必要调用链

源码重建链：

```text
npu_adapter.py -> t079_npu_case.py -> TestPatternMatcher.test_cat_slice_cat_cuda
  -> TestPatternMatcher.common -> torch.compile
  -> torch/_inductor/fx_passes/post_grad.py::cat_slice_cat
  -> torch.testing.assert_close + counter assertions
```

## 4. 结果与边界

1/1 tests PASS，三条原社区分支的 matcher/node 断言和数值均通过；功能适配无产品代码变更。性能
使用正例合同派生的目标级 OFF/ON 测例，Event p50 改善 18.41%，判定保留启用。完整证据见
[`复现报告.md`](复现报告.md)。
