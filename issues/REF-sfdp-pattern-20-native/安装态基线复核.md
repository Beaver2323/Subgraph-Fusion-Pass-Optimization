# T-105 pattern 20 安装态基线复核

> 证据时间：2026-09-14T22:39:24.304408+08:00；复核时间：2026-09-14T22:42:00+08:00。

## 1. 本轮结论与适配范围

原方法 tests=1/skip=0；已通过Tensor比较=0；本编号精确改写=1；原社区合同通过，仍待逐项图/性能审核；没有数值比较证据，不计精度PASS。

GPU已确认本编号；NPU固定triton_experimental。原生类定义阻断已有原件，随后执行本case适配。
保留原方法、dtype/shape/训练推理参数和数值容差；CUDA代码符号断言显式适配为实际NPU调用。
未改安装态产品、未加载注册候选、未签性能门禁。详见[适配报告](适配报告.md)。

## 2. 原代码合同与必要调用链

```text
test/inductor/test_fused_attention.py::SDPAPatternRewriterGpuTests.test_sdpa_rewriter_20_gpu
 -> _check_common -> torch.compile -> AOT joint_graph -> fuse_attention pattern
 -> backend lowering/codegen -> NPU -> 原counter/codegen/数值断言
```

```python
# 冻结 test/inductor/test_fused_attention.py:121、137、140（节选）
self.assertGreaterEqual(counters["inductor"]["fuse_attention"], 1)
if not has_dropout or override_check_equal:
    self.assertEqual(result1, result2, atol=atol, rtol=rtol)
# training分支执行backward；相同条件成立时才比较浮点输入梯度。
```

数值比较为零时不能记精度通过；未执行的后续参数分支不借GPU结果补成NPU通过。

## 3. 实际目标与生成代码线索

实际发生图变化的注册：`_sfdp_pattern_20_inference`。

实际debug生成图数量：1。下表是AST读取的调用，不是性能测量或完整fallback判决。

| 生成调用 | 图内静态出现次数 |
|---|---:|
| `triton_unk_fused_tril_0.run` | 1 |
| `triton_unk_fused_ones_1.run` | 1 |
| `torch.ops.aten.logical_and.default` | 1 |
| `triton_unk_fused_eq_expand_logical_not_npu_fusion_attention_v3_transpose_view_2.run` | 1 |
| `torch.ops.npu.npu_fusion_attention_v3.default` | 1 |

可疑CPU行数量：0；无可疑字符串不自动等于无fallback。

## 4. 原失败栈与后续工作

原方法无失败/异常；仍需按原断言范围审查，不把结构通过外推成全精度/性能通过。

[本轮原件](evidence/t105-native-jq9qae5o/adapter/)包含 result、执行器快照、目标FX、前后IR和output_code。

[性能测例来源与数值边界](../../report/attention_performance_contract_review_20260914.md)。
