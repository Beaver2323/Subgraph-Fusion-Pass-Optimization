# REF-addmm-contract-native NPU 复现报告

> 更新时间：2026-09-02 05:32 CST（UTC+08:00）
> 当前阶段：已归类，待修复
> 结果分类：`NPU_REGRESSION`

- 原始测试：`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_addmm`
- 目标单元：`AU-post-grad-addmm`
- 计划：原生优先；adapter 直接复用完整上游 test body，仅将模块 `GPU_TYPE` 设为 `npu` 并预选 `triton_experimental`。
- 覆盖：matrix/vector bias 两种 operand order 正例，以及 non-expandable/batched/Python scalar 负例。
- 原生结果：`NO_TESTS`。
- 最小 adapter：执行完整上游 5 个逻辑 case，所有 eager/compiled 数值断言通过；测试因目标 counter 合同失配正常失败。
- 正例：matrix/vector bias 两类的两种 operand order，GPU 预期 `count/nodes=2/4`，NPU 实际均为 `0/0`。
- 负例：non-expandable、batched、Python scalar 均为预期的 `0/0`。
- 失败图检查：FX 仍是两路 `mm + add`；`output_code.py` 是两个 extern mm 加 NPU Triton pointwise add，没有 addmm replacement。`disable_addmm_fusion=False`，因此不是预期 gate。

### Handoff

- **阶段结论**：首个分歧在 pattern/replacement 阶段，正式结论为 `NPU_REGRESSION`。
- **下一步动作**：已进入 repair queue；修复 NPU addmm pattern registration/lowering 后重跑。
