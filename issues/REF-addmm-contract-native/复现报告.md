# REF-addmm-contract-native NPU 复现报告

> 更新时间：2026-09-02 17:42 CST（UTC+08:00）
> 当前阶段：安装态基线已纠偏，P-018 候选已验证
> 结果分类：安装态 `EXPECTED_PRODUCT_DIVERGENCE`；候选 `verified`

- 原始测试：`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_addmm`
- 目标单元：`AU-post-grad-addmm`
- 计划：原生优先；adapter 直接复用完整上游 test body，仅将模块 `GPU_TYPE` 设为 `npu` 并预选 `triton_experimental`。
- 覆盖：matrix/vector bias 两种 operand order 正例，以及 non-expandable/batched/Python scalar 负例。
- 原生结果：`NO_TESTS`。
- 最小 adapter：执行完整上游 5 个逻辑 case，所有 eager/compiled 数值断言通过；测试因目标 counter 合同失配正常失败。
- 正例：matrix/vector bias 两类的两种 operand order，GPU 预期 `count/nodes=2/4`，NPU 实际均为 `0/0`。
- 负例：non-expandable、batched、Python scalar 均为预期的 `0/0`。
- 失败图检查：FX 仍是两路 `mm + add`；`output_code.py` 是两个 extern mm 加 NPU Triton pointwise add，没有 addmm replacement。
- 运行态纠偏：测试进程没有从 dirty source tree 导入 torch_npu，而是从 Pass site-packages 加载；安装态 `config.py` SHA256 为 `68ae6164...f763b`，实际 `disable_addmm_fusion=True`。后端激活时明确把两个 addmm entry 的 `extra_check` 置为 False，因此 0/0 是产品 gate，不是未知 pattern 回归。
- P-018 候选：独立 wheel SHA256 `cd301382...3f3452`，默认 `disable_addmm_fusion=False` 并安装 live opt-out wrapper。冻结 PyTorch commit 上完整 5 个逻辑 case 通过：matrix/vector 正例均为 `2/4`，non-expandable/batched/Python scalar 负例均为 `0/0`；正例 `output_code.py` 各含两个 `extern_kernels.addmm`。
- 环境边界：负例 pointwise Triton fresh launcher 使用已登记的 T-022 audit-only host compiler wrapper；它只补齐 editable headers/C++20/CANN 声明，不修改 FX、gate、lowering 或设备数值路径。

### Handoff

- **阶段结论**：安装态首个分歧在显式 config gate，正式结论纠正为 `EXPECTED_PRODUCT_DIVERGENCE`；原 known issue 已移出 open queue。
- **候选结论**：P-018 已按同一上游合同恢复全部正例并保留负例，不需要修改 PyTorch pattern、NPU addmm lowering 或手写 Triton kernel。
- **证据入口**：`results/current/AU-post-grad-addmm/p018_candidate_result.json`。
