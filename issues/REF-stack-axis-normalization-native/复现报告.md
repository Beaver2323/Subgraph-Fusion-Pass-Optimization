# T-091 stack normalization 原生 NPU 复现

> 更新时间：2026-09-14 20:02 CST（UTC+08:00）；原生 NO_TESTS，最小适配后社区合同 PASS。

## 基本信息与用例意图

原入口：`test/inductor/test_split_cat_fx_passes.py::TestSplitCatFxPasses.test_stack_normalization_axis_kwarg`。
两个 `[4,4]` FP32 张量在真实设备执行 `torch.stack([x,y], axis=1)`，原断言比较 compiled/eager。
精确目标为 pre-grad `normalize_stack_default`；图已在更早阶段把 axis 变成 dim，不能将 handler 的节点重建误称为 axis 首次改写。

## 环境信息

要求 Pass / PyTorch `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b` / `triton_experimental`。
从 `/home/z50063656/tmp` 启动；选物理 NPU 5（2026-09-14 预检空闲）。
实际 Python 3.11.15，CANN 9.0.1，Ascend 910B2，torch 2.14.0a0+git8e86e0a，torch_npu 2.14.0a0+git83cc452。
torch_npu 来自 Pass site-packages，不从源码树导入；详细路径、PYTHONPATH 和逐模块 SHA256 在归档 result.json。

## 禁用项与依赖预检

冻结 torch_npu 源码未提供 `test_upsteam/disabled_testcases.json`，如实记录为文件不存在，而不是宣称完成名单内排除。
原测试有 `requires_gpu` 和 main 的 `HAS_GPU` 限制；这些是测试入口条件，不是 NPU 产品显式 disable。
本例依赖 stack、编译与 pre-grad normalization；无通信和模型参数。

## 复现命令

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/run_prepared_npu_case.py \
  --task T-091 --case REF-stack-axis-normalization-native --npu 5
```

## 关键环境变量

启动器在导入前设置 `TORCHINDUCTOR_NPU_BACKEND=triton_experimental`、`ASCEND_RT_VISIBLE_DEVICES=5`，独立编译缓存。

## 执行结果

原生返回码 0 但 tests=0；原件 `native_runs/native-20260914T195935-69hp7cbl/`。
按 [最小适配报告](适配报告.md) 执行同一原方法后：1 test / 0 skip / exit0，原 NPU eager/compiled assertEqual 通过，
精确 normalize_stack_default 一次。没有产品修复，也没有性能收益结论。

## 首个有效报错栈、SKIP 与 fallback

无 exception traceback；原生 main 的 HAS_GPU 阻断见源码第 1566 行。
适配后没有 CPU fallback 告警。日志中的 torchair 未编译警告是另一 Dynamo backend 的注册通知，
本例已由注册器和 output_code 证明使用 Inductor experimental，不是 eager 替代。

## 初步判断与修复方向

优先保留原生阻断；仅在确认入口不接受 NPU 后，使用保持原方法和断言的最小设备适配。
不修改冻结 PyTorch 或产品配置，不签性能 gate。

## 下游处理与 Handoff

- 阶段结论：PASS（社区功能阶段，非正式全流程闭环）。
- 下一步动作：本单元独立 OFF/ON 功能/性能门禁；其他已归因 attention 单元逐例进入 NPU。
- 传递工件：[FX/IR/output_code 与 result](evidence/t091-native-y2vojco3/adapter/)、native_runs/adapter_runs 原日志。
- 阻塞条件：无功能阻塞；未执行性能、未签发正式 comparison。
