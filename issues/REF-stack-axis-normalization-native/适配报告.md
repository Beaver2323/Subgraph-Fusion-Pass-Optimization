# T-091 stack normalization 测试入口最小适配

> 更新时间：2026-09-14 20:02 CST（UTC+08:00）；单元：AU-split-cat-normalize-stack-default。
> PyTorch：8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b；后端：`triton_experimental`；状态：原社区功能 PASS，未签性能 gate。

## 1. 原生阻断

`test/inductor/test_split_cat_fx_passes.py::TestSplitCatFxPasses.test_stack_normalization_axis_kwarg`。
native_runs/2026-09-14 记录为 NO_TESTS；main 的 HAS_GPU 未放行 NPU。未发现明确产品 disable。

## 2. 最小适配：adapter_deviation 与 preserved_contract

```python
# issues/REF-stack-axis-normalization-native/npu_adapter.py
with mock.patch.object(triton_utils, 'requires_gpu', lambda f: f):
    spec.loader.exec_module(community)
community.GPU_TYPE = 'npu'
# 显式实例化原方法，保持其 config patch、shape=[4,4]、FP32、axis=1 和 assertEqual。
```

只替换设备入口、GPU 装饰器及 main 测试发现；先确认真实 NPU 和实际 backend。
原图、输入、dtype、stride、正例语义和社区数值断言不改。
额外观察 GraphPatternEntry.apply 已过 guard 的原返回及 FX 前后，记录原 assertEqual 的张量设备。
`product_gate_bypassed=false`；未改产品源码，不构造性能 ON 路径。

## 3. 必要调用链（源码重建，非异常 traceback）

```text
npu_adapter.py → 原 TestSplitCatFxPasses 方法 → torch.compile
 → pre-grad normalization_pass → normalize_stack_default
 → lowering/codegen → NPU → 原 compiled/eager assertEqual
```

## 4. 结果与边界

GPU 精确 handler 已观察到，但边界前后均为 dim=1，节点重建不是新的 axis→dim 语义变化。
不把文本节点名变化称为计算优化，不宣称模型端到端收益。NPU 1/1、零 skip，原 assertEqual 的两侧均为
NPU FP32 `[4,2,4]`，目标 handler 一次；与 GPU 的节点重建合同一致。
生成代码使用 NPU 图内 `aten.cat.default` 加 `reinterpret_tensor`，不是 Triton 融合核，也不是 CPU fallback。
完整 FX、IR、output_code 和版本/加载源码 SHA256 见 [归档原件](evidence/t091-native-y2vojco3/adapter/)。
原生阻断和适配日志分别见 `native_runs/native-20260914T195935-69hp7cbl/`、`adapter_runs/native-20260914T200142-3r5o5kcd/`。
launcher 通用字段 backend_verified=false 表示启动器未判定后端；adapter 的 result.json 单独通过注册器确认真实后端，不改写原启动记录。

## 执行与后续门禁

从 tmp 使用 `scripts/run_prepared_npu_case.py --task T-091 --case REF-stack-axis-normalization-native --npu 5 --adapter`。
此 adapter 不签发正式 comparison 或性能 gate；需原日志、数值、FX/IR/output_code 复核后推进。
