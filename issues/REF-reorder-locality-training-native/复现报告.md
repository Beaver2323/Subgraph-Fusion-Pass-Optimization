# T-087 训练重排原生测例 NPU 复现

> 更新时间：2026-09-11 12:22 CST（UTC+08:00）
> 当前阶段：原生 CUDA 类未生成；最小设备适配后三进程训练合同通过，性能待办。

## 基本信息

- 原始测试：`test/inductor/test_reorder_for_locality_in_training.py::TestReorderForLocalityInTrainingCUDA.test_training_flag_reorders_and_preserves_semantics_cuda`。
- 意图：训练图 ON 确认重排实际移动节点，OFF 不调用；比较梯度和 optimizer step 后参数。
- GPU 原生 reference：T-087 完整包已校验，测试通过，无 skip。
- 上游源码：`/home/z50063656/Pass/src/pytorch`，冻结 commit `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。
- 禁用列表：当前 torch_npu 源码未提供 `test_upsteam/disabled_testcases.json`，不能假定已禁用；当前 experimental config 未发现目标显式 disable。

## 环境信息

Pass Python 3.11.15；torch `2.14.0a0+git8e86e0a`，实际 editable 源码为上述冻结位置；torch_npu `2.14.0a0+git83cc452` 从 Pass site-packages 加载；CANN 9.0.1；物理 NPU 2（910B2）。未修改冻结 PyTorch 源码和安装态产品包。

## 复现命令

从 `/home/z50063656/tmp` 启动，导入前选择 `TORCHINDUCTOR_NPU_BACKEND=triton_experimental`；原生入口优先，阻断后才进入最小测试适配。

## 关键环境变量

`TORCHINDUCTOR_NPU_BACKEND=triton_experimental`、`ASCEND_RT_VISIBLE_DEVICES=2`、`SET_NPU_DEVICE=0`，每臂独立进程和缓存目录。不得在 torch_npu 源码目录导入 torch。

## 执行结果

原生返回码 1，`_FailedTest` 提示未生成 CUDA 测试类，其“Ran 1 test”是 loader 失败占位，非测试体执行。随后 OFF/ON/master-off 三臂退出码均为 0，实际执行训练前后向与 optimizer step。ON 目标调用 2 次且反向图移动；另两臂零调用；参数和梯度跨进程比较全部通过，8 项 max-abs-error=0。

## 初步判断

仅 GPU 测试 harness 阻断；没有发现该社区输入上的 NPU 产品功能缺陷。正例与主开关关闭负例均有实际 NPU 证据，不能把测试框架阻断写成算子不支持。

## 修复方向建议

本轮只做测试设备、独立进程及只读 FX 采集适配，不更改产品 gate 或冻结源码。具体代码与调用链见 [适配报告](适配报告.md)。

## 下游处理与 Handoff

结果和可审阅原件见 [训练复核 JSON](../../results/current/T-087/npu_training_review.json)、[三臂 FX/IR/生成代码](evidence/npu-20260911/)。剩余为 fallback/graph-break 人工复核、性能测量图门禁及独立计时；T-087 另一个设备解析单元不在此结果范围内。
