# REF-splitwithsizes-cat-native NPU 最小适配报告

> 更新时间：2026-09-07 18:45 CST（UTC+08:00）
> 任务/单元：T-079 / `AU-post-grad-splitwithsizes-cat-replace`
> 后端：`triton_experimental`

## 1. 原生阻断

上游模块在 `HAS_GPU=false` 时不运行 unittest；NPU 原生命令为 0 tests。

## 2. 最小适配

```python
# issues/REF-splitwithsizes-cat-native/npu_adapter.py
runner = Path(__file__).parents[2] / "runners" / "t079_npu_case.py"
sys.argv = [str(runner), "--case-id", "REF-splitwithsizes-cat-native", *sys.argv[1:]]

# runners/t079_npu_case.py
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
```

`adapter_deviation`：设备注入、原类显式实例化、测试诊断插桩关闭。`preserved_contract`：正例及缺片、
异维、重排三负例的输入、计算、数值和 counter 判据均保持。`product_gate_bypassed=false`。

## 3. 必要调用链

源码重建链：

```text
npu_adapter.py -> t079_npu_case.py -> test_splitwithsizes_cat -> common
  -> torch.compile -> post_grad.is_valid_splitwithsizes_cat
  -> post_grad.splitwithsizes_cat_replace -> original assertions
```

## 4. 结果与边界

1/1 tests PASS；正例和三个 guard 的原 counter/node 判据均通过，故不是仅凭数值判定。性能正例
Event p50 改善 28.46%，保留启用，无产品修复。背景与工件见[`复现报告.md`](复现报告.md)。
