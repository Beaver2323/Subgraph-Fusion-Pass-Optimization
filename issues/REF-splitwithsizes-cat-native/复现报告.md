# REF-splitwithsizes-cat-native NPU 复现报告

> 更新时间：2026-09-07 18:45 CST（UTC+08:00）
> 状态：功能 PASS，性能 `PERF_IMPROVED`
> 后端：`triton_experimental`

## 1. 社区合同

社区入口：`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_splitwithsizes_cat`，冻结
PyTorch `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。完整、同序、同 dim 的 split→cat 可直接
返回输入；缺片、异维和重排分别必须拒绝替换。

```python
# PyTorch: test/inductor/test_pattern_matcher.py:981
parts = torch.ops.aten.split_with_sizes.default(a, [8, 24], 1)
cat = torch.ops.aten.cat.default([parts[0], parts[1]], 1)
return cat**2
```

## 2. 原生复现

原模块因 NPU 不计入上游 `HAS_GPU` 而执行 0 tests，结果分类 `NO_TESTS`。

## 3. 最小适配与调用链

见 [`适配报告.md`](适配报告.md)。社区方法 → `common()` → `torch.compile` →
`is_valid_splitwithsizes_cat` → `splitwithsizes_cat_replace` → 原数值/counter 断言。

## 4. 动态结果

1/1 tests PASS。正例 counter 为 `[1,4]`，缺片、异维、重排三个负例均为 `[0,0]`，原数值断言全部
通过。目标级 6 臂结果：NPU Event p50/p99 改善 28.46%/26.46%，host p50/p99 改善
31.28%/34.91%，结论 `PERF_IMPROVED`，无需产品修复。

功能工件：`/home/z50063656/tmp/t079-npu-results/REF-splitwithsizes-cat-native/adapter-0mdctuz8/`；性能工件：
`/home/z50063656/tmp/t079-performance-results/split-cat-performance-20260907T101115+0800/`。
