# T-102 pattern 1 最小测试适配

> 更新时间：2026-09-14 20:35 CST（UTC+08:00）；实际后端要求：`triton_experimental`。

## 1. 原生阻断

原入口 `SDPAPatternRewriterGpuTests.test_sdpa_rewriter_1_gpu` 因 class 定义条件未满足，返回 `_FailedTest` / AttributeError，未执行测试体。
原件：`native_runs/native-20260914T203214-xzk84n9l/`。冻结 commit 为 `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。

## 2. 最小适配

本目录 `npu_adapter.py` 调用公共 `runners/attention_community_npu.py`，不修改 PyTorch 或安装包。

```python
# runners/attention_community_npu.py：仅导入期间定义社区类
with mock.patch.object(inductor_utils, 'HAS_CUDA_AND_TRITON', True), \
     mock.patch.object(common_cuda, 'PLATFORM_SUPPORTS_FUSED_ATTENTION', True):
    spec.loader.exec_module(community)
# 类的原测试方法保持不变，实际 tensor 在 NPU 上创建。
class NpuCase(community.SDPAPatternRewriterGpuTests):
    device = 'npu'
```

保留 FP32/FP16、原 shape/stride、训练/推理、checkpoint、数值容差及梯度断言。
只读 replacement 观察器记录经过 extra_check 后的真实 before/after；其 device_modified=false 仅描述观察器本身，不掩盖 adapter 的 CUDA→NPU 设备变化。

## 3. 必要调用链（源码重建）

```text
npu_adapter.py → 原 _test_sdpa_rewriter_1 → _check_common
→ torch.compile → joint_graph → SFDP replacement → lowering/codegen
→ NPU → 原输出、输入梯度与代码路径断言
```

## 4. 结果与边界

第一轮最小设备适配在CUDA代码符号断言失败，原件位于 `adapter_runs/native-20260914T203355-qc7okxbc/`。
实际代码包含 `torch.ops.npu.npu_fusion_attention_v3.default(...)`，不是CPU fallback，也不是CUDA同名kernel。
第二轮只将代码符号断言映射到AST确认的NPU fusion-attention调用，不能用注释或字符串充数。
原数值/梯度容差与测试体不变；明确记录 `codegen_assertion_modified=true`，不声称所有断言原封不动。
安装态在训练首次目标计数断言失败；隔离训练注册候选已通过完整原方法的20次数值/梯度比较与8次精确改写。
候选未部署、未签性能门禁；详见[根因分析](根因分析.md)与[候选验证](修复验证报告.md)。`product_gate_bypassed=false`。
