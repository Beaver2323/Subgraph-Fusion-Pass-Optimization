# T-102 训练注册窄部署

> 本文是 2026-09-15 的部署记录，保留当时的提交和测试结果。PR 46464 现已推送并取消草稿，CLA 通过；最新验证见[合入说明](../代码合入描述.md)，远端进度见[流水状态](../../../report/四个PR推送与流水状态_20260921.md)。

> 更新时间：2026-09-15 19:42 CST（UTC+08:00）。已部署；五个原方法与六项边界安装态复验通过，性能仍独立验收。

依据用户“候选和边界通过后备份、仅修改 triton_experimental、无候选新进程复验”的授权执行。

## 部署前条件

同一产品化候选下五个完整原方法全部通过，原数值断言不变；真实设备六项边界通过，共 25 次输出/梯度比较。三个负例（中间结果复用、tensor scale、scalar mask）均未命中 attention。静态测试确认 CPU/CUDA、推理、未审查编号和混设备保持原入口，重复激活幂等。具体原件及哈希见 [preparation.json](preparation.json)。

## 实际修改

1. 新增 `/home/z50063656/envs/Pass/lib/python3.11/site-packages/torch_npu/_inductor/triton_experimental/sfdp_training.py`，逐字等于实测候选。
2. 同目录 `__init__.py::_activate` 在既有注册完成后新增该模块的导入和激活调用。不改变其他 backend 或产品开关。

| 文件 | 修改前 SHA256 | 修改后 SHA256 |
|---|---|---|
| `__init__.py` | `d11e1c9a3da36633e0a8d4d7baecc119140b8b3fff2b11a09b0947056cbc3538` | `8aa275ca3ecba7d3b609cbaad6076f16c46f54c4603e73777efc11676f2011b9` |
| `sfdp_training.py` | 原不存在 | `7feb336e69df3dfd2a0d376ae8b9e2c26e1b1363b26580fa5c8839a67b3e588a` |

`codegen/triton.py` 保持 `86f310ce99960eb12ead8b67db7264c068a56f9ecb5e0e7a43a6e48cbcad5a57`，没有覆盖 T-106 修复。没有修改冻结 PyTorch、没有 C++ 改动、没有重建 wheel 或发起产品 PR。

## 备份与恢复边界

原激活文件完整备份为 [before-__init__.py](before-__init__.py)。回退时必须先停止本项目相关新启动任务，确认当前两个目标文件仍与本次修改后哈希一致，再恢复原激活文件并移走新增模块；若存在后续变更，先合并评审，禁止覆盖恢复。这里只描述回退范围，本轮没有执行回退。

## 验证状态

物理 NPU 5，cwd `/home/z50063656/tmp`，导入前指定 `triton_experimental`。五个原例逐项新进程运行，边界另启新进程，命令不含任何 candidate 开关。原例 Tensor 比较分别为20/10/0/0/30次，精确本编号改写8/4/2/2/8次；3/4原方法没有数值oracle，不伪称精度全通过。边界三个正例命中、三个负例不误改写，共25次输出/梯度比较通过。

最终证据见 [deployment.json](deployment.json)，已由只读部署校验器核验。独立产品分支 `fix/t102-training-registration-20260915` 本地提交 `3a0b8179d`，基于83cc452，不修改脏的源工作树；未推送产品仓、未运行社区CI，也未验证干净wheel。实际测试为当前Pass安装态（含既有已记录修复），不冒称干净83cc环境。
