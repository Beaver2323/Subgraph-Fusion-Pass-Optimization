# T-102 pattern 1：NPU 原社区合同复现

> 更新时间：2026-09-14 20:38 CST（UTC+08:00）；阶段结果 FAIL：训练目标不命中。

## 基本信息

冻结 PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。
原入口：`test/inductor/test_fused_attention.py::SDPAPatternRewriterGpuTests.test_sdpa_rewriter_1_gpu`。
GPU 精确目标已复核；本轮验证 FP32/FP16、训练/推理、checkpoint 和社区数值/梯度断言。

## 环境信息

Pass 已安装环境；从 `/home/z50063656/tmp` 启动；导入前选择 `triton_experimental`。
910B2 物理 NPU 5 空闲；不修改 PyTorch 源码或安装包。

## 原生执行

执行前已检查指定的 `test_upsteam/disabled_testcases.json` 路径，该文件不存在；不声称完成其他禁用清单审阅。
原方法的 class 定义依赖 CUDA/XPU + fused-attention 测试环境，先保存其实际入口结果。

## 最小适配

原生 `_FailedTest` 已确认；首次设备适配失败于CUDA生成代码字符串断言。
实际 NPU 调用 `torch.ops.npu.npu_fusion_attention_v3.default`；第二轮只适配代码路径断言为AST实际调用检查，
数值与梯度断言不变。原测试体与产品 gate 不变，详见适配报告。

## 执行结果和必要调用链

第二轮实际run：`adapter_runs/native-20260914T203509-akluzi8x/`，原始图在
`/home/z50063656/tmp/t102-native-oen9jamj/adapter/`。推理FP32命中、输出检查通过；随后训练FP32计数为0。

```text
# stderr.log 实际 traceback（省略unittest外层包装）
test_fused_attention.py:172 _test_sdpa_rewriter_1
  → test_fused_attention.py:121 _check_common
  → assertGreaterEqual(counters["inductor"]["fuse_attention"], 1)
AssertionError: 0 not greater than or equal to 1
```

1个方法失败，无skip；不得声称FP16或梯度比较已完成。生成了推理与训练forward代码；未发现CPU fallback警告。

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/run_prepared_npu_case.py \
  --task T-102 --case REF-sfdp-pattern-1-native --npu 5 --adapter --timeout 600
```

环境：CANN9.0.1、Python3.11.15、Pass安装torch_npu、NPU910B2；实际版本/加载文件及SHA见原始result.json。

## 下游处理

阶段结论：FAIL；进入图模式诊断。未签性能门禁，未修改产品源码或安装环境。
