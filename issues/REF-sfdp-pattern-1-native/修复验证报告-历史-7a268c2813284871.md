# T-102 pattern 1 隔离候选验证

> 更新时间：2026-09-14 20:47 CST（UTC+08:00）；初验通过，未部署产品包。

## 问题描述

当前NPU推理命中，训练counter=0，原社区断言FAIL；首个分歧是预生成训练pattern与NPU分解不一致。
详见[根因分析](根因分析.md)，其中包含具体代码和调用链。

## 修复内容摘要

`runners/attention_registration_candidate.py`在独立进程替换指定SFDP训练注册生成方式，
按实际NPU decomposition生成匹配图；不改安装包、PyTorch源码、原search/replacement/extra_check。
只有Python候选，没有C/C++变更；真正产品集成位置尚待评审。

## 验证结果

| 范围 | 当前安装态 | 隔离候选 |
|---|---|---|
| 原test_sdpa_rewriter_1_gpu完整方法 | 训练FP32首次counter断言失败 | 1/1 PASS、0skip |
| FP32/FP16，推理/训练，普通/checkpoint | 仅初始推理完成 | 8次精确改写，20次原Tensor比较通过 |
| 前向/反向codegen | 未融合的训练图 | npu_fusion_attention_v3 / npu_fusion_attention_grad_v3 |
| 安装包 | 未改 | 前后快照一致，候选不等于已部署 |

近邻用例：尚未进行；这不是整族验证完结。C/C++编译：不适用。

## 代码变更审查

```diff
# runners/attention_registration_candidate.py，概念差异；完整候选见源码
- 读取预生成的training PatternExpr
+ 仅NPU目标training：按当前NPU decomposition trace出PatternExpr
  保留search_fn、replace_fn、extra_check、scalar_workaround、skip_duplicates
```

没有删除精度保护，也不改变数值容差。相比恢复CUDA backward decomposition，候选只解决注册图形态不兼容。

## 验证命令与关键输出

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/run_prepared_npu_case.py \
  --task T-102 --case REF-sfdp-pattern-1-native --npu 5 --timeout 600 \
  --adapter --attention-registration-candidate
```

结果为adapted-test-passed-awaiting-contract-review；[实际日志](candidate_runs/native-20260914T204029-hu3wzlhx/stderr.log)。
[前后工件](evidence/)含output_code.py、ir_pre_fusion.txt、ir_post_fusion.txt及两类FX。
观察器中的numerical_correctness_proven_by_observer=false只表示观察器自身不检查数值；数值来自原unittest断言。

## 残余风险与 Handoff

未验证其他编号、BF16、大shape、候选性能和安装态产品集成；没有创建fix分支或PR。
本项目按隔离候选流程执行，不把通用verify skill的“安装修复包”前提误写成已满足。
阶段结论：初验通过；后续动作：邻接回归与产品化评审；传递工件：本报告、根因分析、候选源码和完整前后工件。
风险important，功能/性能整项未闭环。
