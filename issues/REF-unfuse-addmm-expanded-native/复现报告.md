# REF-unfuse-addmm-expanded-native NPU 复现报告

> 更新时间：2026-09-06 08:09 CST（UTC+08:00）
> 阶段结论：`PASS`（stride-0 expanded bias 正确拆分）
> NPU 后端：`triton_experimental`

## 测试合同

上游方法：
`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_unfuse_expanded_bias_addmm`；
冻结 PyTorch commit `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。

FP32 bias `[20]` 经 `unsqueeze(0).expand(10,20)` 形成 stride-0 bias，随后执行
`relu(addmm(bias, [10,15], [15,20]))`。社区原断言要求：

- expanded bias 的 stride(0) 为 0；
- compiled 与 eager 数值一致；
- generated code 不含 `extern_kernels.addmm`。

GPU 紧凑 reference 中该 case 通过且 FX before/after 签名不同。

## 原生入口与最小适配

原生入口受 `HAS_GPU` 启动门阻断，退出 0 但 0 tests，日志为
`direct-qbtry4vc/`。最小适配仅设置 `GPU_TYPE=npu` 并显式运行原方法，没有修改
expand/stride、shape、数值或 FileCheck。

## 结果

```text
Ran 1 test in 32.067s
OK
```

有效工件：
`/home/z50063656/tmp/t078-npu-results/REF-unfuse-addmm-expanded-native/adapter-tcrvicoc/`。

transformed FX：

```python
# test/inductor/test_pattern_matcher.py:1653-1654
unsqueeze = torch.ops.aten.unsqueeze.default(arg2_1, 0)
expand = torch.ops.aten.expand.default(unsqueeze, [10, 20])
mm_default = torch.ops.aten.mm.default(arg0_1, arg1_1)
add_tensor = torch.ops.aten.add.Tensor(expand, mm_default)
relu = torch.ops.aten.relu.default(add_tensor)
```

output code 仅调用 `extern_kernels.mm(...)`；expand、bias add 与 ReLU 进入
`triton_unk_fused_addmm_expand_relu_unsqueeze_0`，没有 extern addmm。原 stride、数值、
codegen 断言均通过，0 skip、无 fallback、无需源码修复。

执行命令：

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
export ASCEND_RT_VISIBLE_DEVICES=0
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/issues/REF-unfuse-addmm-expanded-native/run_native.py --adapter
```

---

### Handoff

- **阶段结论**：PASS
- **目标行为**：stride-0 expanded bias 拆为 mm + 可融合 pointwise
- **下一步动作**：执行 half dtype preserve/flag-disabled 两组 gate
- **传递工件**：runner、adapter、FX/IR/output_code
- **阻塞条件**：无
