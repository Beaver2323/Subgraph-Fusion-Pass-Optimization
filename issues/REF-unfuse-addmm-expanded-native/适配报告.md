# REF-unfuse-addmm-expanded-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-unfuse-bias-add-to-pointwise`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：expanded-bias 原方法实例化；`product_gate_bypassed=false`

## 1. 原生阻断

`TestPatternMatcher.test_unfuse_expanded_bias_addmm` 受 `HAS_GPU` 启动门阻断。该 case 验证 expand 后
bias 的结构和语义，不能用普通 vector bias 替代。

## 2. 最小适配

```python
# issues/REF-unfuse-addmm-expanded-native/npu_adapter.py:42-48
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
suite = unittest.TestSuite([NpuCase(METHOD)])
```

原方法、expand、shape、数值及 FileCheck 保持；只更换设备并显式实例化，未解除产品关闭项。

## 3. 必要调用链

源码重建链：`NpuCase.test_unfuse_expanded_bias_addmm → torch.compile → unfuse matcher/guard
→ NPU generated path → correctness/FileCheck`。

## 4. 结果与边界

适配合同执行完成；正式产品决定仍为 `EXPECTED_PRODUCT_DIVERGENCE`，不因 adapter 可运行而宣称
rewrite 已启用。FX/IR/output_code 位置见[复现报告](复现报告.md)。
