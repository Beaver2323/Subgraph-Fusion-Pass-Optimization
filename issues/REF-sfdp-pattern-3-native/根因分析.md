# Pattern 3：dropout 训练注册与实际 NPU 分解不一致

> 更新时间：2026-09-15 19:07 CST（UTC+08:00）。隔离候选已通过，产品化候选与部署验证继续中。

## 测试目的

`softmax(QKᵀ / 3) → dropout(p=0.4) → V` 应进入 SFDP 训练改写，保留训练/推理和 checkpoint 执行。社区原例不比较 dropout 输出/梯度，不能宣称精度 PASS。推理时 dropout 被关闭，由 pattern 1 接替属于原有去重行为；本编号必须看训练改写。

## 根因类型与判定依据

图匹配行为不一致。原安装态在第一个训练分支 `fuse_attention=0` 失败；不是 NPU 算子运行异常。原栈和 FX/IR/output_code 保存在[安装态复核](安装态基线复核.md)。

```text
# 冻结 PyTorch test/inductor/test_fused_attention.py:336、121，实际失败栈摘要
_test_sdpa_rewriter_3 → _check_common → torch.compile → AOTAutograd joint graph
 → fuse_attention._sfdp_init → gen_register_replacement（读取 CUDA 预生成图）
 → 原 assertGreaterEqual(fuse_attention, 1)
 → AssertionError: 0 not greater than or equal to 1
```

```python
# evidence/t102-native-3aj8a5x5/adapter/registration-candidate/target-_sfdp_pattern_3_training.py
# 实际 NPU trace 节选，不是拟造的预期图。
_npu_dropout = torch.ops.npu._npu_dropout.default(div_1, 0.113377)
matmul_backward = torch.ops.aten.matmul_backward.default(tangents_2, getitem_1, primals_6, [True, True])
npu_dropout_backward = torch.ops.npu.npu_dropout_backward.default(getitem_5, getitem_3, 0.11337700000000006)
# 后续 softmax 梯度是 mul/sum/mul/sub，未恢复 FMA。
```

预生成 CUDA 训练注册与 NPU dropout、matmul_backward、softmax 梯度分解形态不同。保持 NPU 分解不变，按同一 search 函数重新生成训练 pattern 后，原断言通过。这是注册层修复证据，不说明 Triton 编译器缺少 attention 语法。

## NPU 能力匹配评估

本轮 6 份实际生成图包含 NPU fusion-attention 前向及 grad-v3 反向调用；没有以 CPU 计算替代。原例 1/1、零 skip、两次本编号训练改写、零 Tensor 比较。函数可运行与随机性/精度全面验证严格分开。

## 修改文件清单与关键变更

当前只使用 `runners/attention_registration_candidate.py` 进程内候选，未部署。

```python
# runners/attention_registration_candidate.py:23
pat, gm = pm.gen_pattern_and_search_gm(search_fn, inputs, trace_fn, scalar_workaround)
# 原 replace_fn / extra_check / skip_duplicates 保留。
pm.register_replacement(**kwargs, pattern_name=name, search_fn_pattern=pat)
```

产品化拟在 `torch_npu/_inductor/triton_experimental/sfdp_training.py` 实现，仅惰性激活时包装 1～5 号实际 NPU 训练注册；不修改 PyTorch、推理入口或其他编号。完整候选见[源码](../REF-sfdp-pattern-1-native/candidate/sfdp_training.py)。

## 修复理由及初步验证

NPU 特有分解是精度/设备实现选择，应在对应后端重建匹配图，而非为了匹配 CUDA 图恢复 FMA 或强分解梯度。
[实际运行命令、完整日志、退出码](candidate_runs/native-20260915T185529-114oidqs/)；[修复前后生成证据](evidence/t102-native-3aj8a5x5/adapter/)。返回码 0，`Ran 1 test` / `OK`。当前不签安装态性能 gate。

## 硬红线自检结果

| 检查项 | 结果 | 依据 |
|---|---|---|
| CPU 兜底 | PASS | 原件调用为 NPU attention/grad；候选没有设备迁移 |
| disabled_testcases 新增 | PASS | 无新增禁用或 skip，不绕过产品 disable |
| 无无关变更 | PASS | 隔离候选仅本编号 NPU training；安装包未改 |
| 根因-修复一致性 | PASS | 实际分解重建训练匹配图，保留原替换和 guard |

## 下游

产品化边界、安装态原方法与性能待验证；不得由低非零 dropout 性能微图推断 p=0.4 的逐元素/随机性一致。
