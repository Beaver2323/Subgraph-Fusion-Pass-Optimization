# T-102 pattern 5 安装态原失败复现

> 汇编时间：2026-09-15 19:20 CST（UTC+08:00）；原证据时间：2026-09-14T22:18:34.195323+08:00。历史 FAIL，不覆盖为当前状态。

## 基本信息

原测试：`test/inductor/test_fused_attention.py::SDPAPatternRewriterGpuTests.test_sdpa_rewriter_5_gpu`。原 CUDA 类发现及代码符号断言经过明确的 NPU 最小适配，详见[适配报告](适配报告.md)；原数值、训练、梯度与 counter 断言不变。

## 环境信息

Pass Python 3.11 / PyTorch `2.14.0a0+git8e86e0a` / torch_npu `2.14.0a0+git83cc452`，CANN 9.0.1，910B2。冻结 PyTorch 8e86e0a；后端 `triton_experimental`，从 /home/z50063656/tmp 启动。

## 复现命令与关键环境变量

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/run_prepared_npu_case.py \\
  --task T-102 --case REF-sfdp-pattern-5-native --npu 5 --adapter --timeout 900
# controller 在导入前设置 TORCHINDUCTOR_NPU_BACKEND=triton_experimental。
```

这是复现入口；原始物理卡号、完整子进程命令、超时与源码前后指纹以[实际父记录](adapter_runs/native-20260914T221618-hcso_l0j/run_result.json)为准，不由重建示例覆盖。

## 执行结果：FAIL

### 首个有效报错栈

```text
Traceback (most recent call last):
  File "/home/z50063656/Pass/src/pytorch/torch/testing/_internal/common_utils.py", line 3829, in wrapper
    method(*args, **kwargs)
  File "/home/z50063656/Pass/src/pytorch/test/inductor/test_fused_attention.py", line 400, in _test_sdpa_rewriter_5
    self._check_common(sfdp_pattern_5_v1, contains=False)
  File "/home/z50063656/Pass/src/pytorch/test/inductor/test_fused_attention.py", line 121, in _check_common
    self.assertGreaterEqual(counters["inductor"]["fuse_attention"], 1)
AssertionError: 0 not greater than or equal to 1

To execute this test, run the following from the base repo dir:
    python attention_community_npu.py NpuCase.test_sdpa_rewriter_5_gpu

This message can be suppressed by setting PYTORCH_PRINT_REPRO_ON_FAILURE=0

```

无 SKIP 作为 PASS；失败在训练匹配计数。生成代码与 fallback 分析见[安装态复核](安装态基线复核.md)；不把空告警自动认作完整无 fallback 证明。

## 初步判断与修复方向

NPU 训练注册与实际分解不一致，按后端真实 search trace 重建 pattern，保留原替换和 guard，不修改上游或恢复 FMA。详见[根因分析](根因分析.md)。仅 Python 候选，无需 C++ 重编译。

## 下游处理

本轮候选证据与部署新进程验证单独归档，保留本历史失败。安装态最终验收和性能不能以候选结果替代。
