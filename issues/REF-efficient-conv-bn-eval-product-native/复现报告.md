# T-098：原生 Conv-BN 合同的 NPU 精度失败

> 更新时间：2026-09-14 20:58 CST（UTC+08:00）。状态：真实设备失败，未修复、未签性能门禁。

## 1. 输入与运行环境

PyTorch `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`，Pass Python 3.11.15，
物理 NPU 5，进程启动前选择 `triton_experimental`。从 `/home/z50063656/tmp` 启动。
GPU 原方法通过，NPU 首先由于 CUDA 专用复制类未定义而出现 `_FailedTest`；
该入口阻断与下述实际数值失败分开归档。

```bash
# tracker/scripts/run_prepared_npu_case.py（原始运行命令详见 adapter_runs 中的执行原件）
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
export ASCEND_RT_VISIBLE_DEVICES=5
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/run_prepared_npu_case.py \
  --task T-098 --case REF-efficient-conv-bn-eval-product-native --npu 5 --adapter
```

## 2. 原方法执行到了哪里

```python
# PyTorch/test/inductor/test_efficient_conv_bn_eval.py:164、220
@tf32_on_and_off(0.003)
@inductor_config.patch({"efficient_conv_bn_eval_fx_passes": True})
def test_basic(self):
    # 原方法每精度模式为112个参数组合；CUDA TF32双臂合计224，本段仅摘录失败位置。
    out_eager = mod_eager(inp)
    out_optimized = mod_optimized(inp)
    self.assertEqual(out_optimized, out_eager)  # 首个 Conv1d 组合在这里失败
    out_eager.mean().backward()
    out_optimized.mean().backward()
```

前四个 Linear 组合的 8 次输出比较及 4 次目标计数断言通过；第五个组合是
`ConvOp / bias=True / Conv1d / SyncBN=False / decompose_nn_module=False`。
输入 FP32 `[4,3,96]`；Conv1d(3→32,kernel=3,stride=2)，输出 `[4,32,47]`。

- 原方法 1 次、0 skip，运行约 339.5 秒后失败。
- 不匹配 378/6016（6.3%）；最大绝对误差 `6.02230429649353e-05`。
- 原容差 `atol=1e-5, rtol=1.3e-6`，未修改。
- 失败组合未进入 backward；剩余组合未执行。不能记成单模式112组合通过。
- 原方法的 SGD 后输出提供间接梯度证据，不等于逐参数梯度 oracle。

## 3. 必要调用栈与实际生成代码

```text
module_folding_community_npu → 原 test_basic_cuda
 → torch.compile → pre_grad.efficient_conv_bn_eval_pass
 → efficient_conv_bn_eval_graph_transform_inlined → efficient_conv_bn_eval
 → AOTAutograd / triton_experimental codegen
 → extern_kernels.convolution → 原 test_basic:220 assertEqual 失败
```

```python
# evidence/t098-native-eh4e214v/adapter/debug/torch_compile_debug/
# run_2026_09_14_20_47_03_320211-pid_2060793/torchinductor/model__4_forward_17.8/output_code.py
# 摘录：权重/偏置折叠用 Triton；卷积仍执行 NPU convolution，不是 CPU fallback。
buf3 = extern_kernels.convolution(
    primals_7, buf1, stride=(2,), padding=(0,), dilation=(1,),
    transposed=False, output_padding=(0,), groups=1, bias=None,
)
```

## 4. 证据与处理边界

[结果原件](evidence/t098-native-eh4e214v/adapter/result.json)、
[证据清单](evidence/t098-native-eh4e214v/adapter/evidence_inventory.json)、
[适配说明](适配报告.md)。保存了失败前后的目标 FX，以及实际 debug FX/IR/output_code。
后续先隔离 HF32 模式与编译分解差异，不放宽容差，不以裁剪诊断替代完整原方法。
