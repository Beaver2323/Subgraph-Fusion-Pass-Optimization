# REF-pad-mm-stride-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-mm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：测试入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

`PadMMTest.test_pad_mm_output_strides_preserved` 受 GPU-only harness 阻断。该合同不仅比较值，还要求
compiled 输出保持社区测例规定的 stride，不能用 contiguous 输出替代。

## 2. 最小适配

```python
# issues/REF-pad-mm-stride-native/npu_adapter.py:48-64
class NpuStride(unittest.TestCase):
    ...
    compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
    self.assertEqual(actual.stride(), expected.stride())
```

仅把输入设备映射为 NPU 并显式选择 backend；shape、dtype、转置/stride 和数值断言全部保留，产品
padding gate 未改。

## 3. 必要调用链

源码重建链：`NpuStride.test → torch.compile → pad_mm pass/gate → generated NPU path → value assertion
→ stride assertion`。

## 4. 结果与边界

adapter 1/1 通过，值与 stride 合同成立，但目标 padding 未启用；正式 verdict 仍是
`EXPECTED_PRODUCT_DIVERGENCE`。原始时长、adapter/code/log 哈希见[复现报告](复现报告.md)。
