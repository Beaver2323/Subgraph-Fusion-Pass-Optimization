# REF-pad-mm-stride-native NPU 复现报告

> 更新时间：2026-09-02 03:55 CST（UTC+08:00）
> 当前阶段：产品 gate baseline 完成
> 结果分类：`PASS`（adapter；stride contract 有效）

## 基本信息

- 原始测试名：`test/inductor/test_pad_mm.py::PadMMTest.test_pad_mm_output_strides_preserved`
- 目标 acceptance unit：`AU-pad-mm-mm`
- 目标后端：NPU `triton_experimental`

## 原生优先执行

- run root：`/home/z50063656/tmp/t076-npu-results/REF-pad-mm-stride-native/direct-20260902T041000+0800`。
- 退出码 0，但没有 unittest 统计；`HAS_GPU_AND_TRITON` 未包含 NPU，分类为 `NO_TESTS`。

## 最小适配结果

- run root：`.../adapter-20260902T041300+0800`；1 test、0 failure/error/skip，1.945 s。
- 仅注入 NPU device/backend 并绕过 GPU-only big-GPU harness；保留原始图、shape、dtype、
  correctness 与 stride equality 断言。
- 输入 `(3,5) @ (5,2)`；eager 和 compiled 输出 stride 均为 `(2,1)`，数值一致。
- `product_gate_bypassed=false`、`disable_pad_mm=true`、后端加载后 `shape_padding=false`。
- 成功 case 不展开读取 `output_code.py`。

关键 SHA256：direct log `bd0e5b8f764780eb05067989d541637d5db974c9c0e8834a4ce0e9281495afaf`；
adapter `63fb93e9c01ea948d526d84920958711a1f819b30b80499736a1a078a1344420`；
result `78cce91bc462822916890351ff30b049856bc14c1190d144e2699db8eae3bac4`；
generated code `1e6a5608b1dce9db5a10066e7974fb35b93c47d49b8c207d74c9850d25ae024d`；
adapter log `96b8f4db5f9682f4f8d3a45306ed0ec1d23cf49d9d8bda4cc5c898a9b4a86499`。

## 判断

NPU 产品 gate 关闭 pad replacement，但原始 mm 的数值和用户可见输出 stride 合同仍有效；该结果
属于 `EXPECTED_DISABLED` 产品基线下的 regression-contract PASS，不进入 repair queue。

---

### Handoff

- **阶段结论**：`PASS`。
- **下一步动作**：已并入 `AU-pad-mm-mm` 正式 comparison。
- **传递工件**：本报告、adapter、最终 run root。
- **阻塞条件**：无。
