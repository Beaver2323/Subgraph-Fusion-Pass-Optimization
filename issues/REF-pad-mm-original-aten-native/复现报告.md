# REF-pad-mm-original-aten-native NPU 复现报告

> 更新时间：2026-09-02 03:50 CST（UTC+08:00）
> 当前阶段：产品 gate baseline 完成
> 结果分类：`PASS`（adapter；`EXPECTED_DISABLED`）

## 基本信息

- 原始测试名：`test/inductor/test_pad_mm.py::PadMMTest.test_original_aten_preserved_pad_mm`
- 目标 acceptance unit：`AU-pad-mm-mm`
- 上游测试文件：`/home/z50063656/Pass/src/pytorch/test/inductor/test_pad_mm.py`
- 目标后端：NPU `triton_experimental`
- 复现时间：2026-09-02 03:44–03:48 CST（UTC+08:00）。

## 用例功能与依赖

- 测试目的：验证 pad-mm 命中后仍以原始 aten `mm` 命名 Triton template，并检查 pattern counter。
- 核心输入：float16，`(16,255) @ (255,16)`。
- NPU 产品预期：`disable_pad_mm=True`，因此目标 pattern/template 不应出现；原始 mm correctness 必须有效。

## 环境信息

- Python 3.11.15；torch `2.14.0a0+git8e86e0a`；torch_npu
  `2.14.0a0+git83cc452`；CANN 9.0.1；Triton runtime 3.2.0；Ascend910B2。
- 复用 T-076 已核验 Pass 环境；直接轮使用启动前无进程的物理 NPU 2，最终轮使用物理 NPU 3。

## Disabled/Patch 预检

- 当前 checkout 未发现 disabled list、对应 patch 或 torch_npu 镜像；与同文件上一 case 一致。

## 原生优先执行

- run root：`/home/z50063656/tmp/t076-npu-results/REF-pad-mm-original-aten-native/direct-20260902T034400+0800`。
- 退出码 0 但没有 unittest 统计；`HAS_GPU_AND_TRITON` 未包含 NPU，分类为 `NO_TESTS`。

## 最小适配

- 首轮误同时传递 `mode` 与 `options`，在进入 Dynamo 前报互斥参数错误；删除重复 `mode` 后 fresh-process 重跑。
- 最终 run root：`.../adapter-20260902T035000+0800`，1 test、0 failure/error/skip，20.389 s。
- 偏离仅为 NPU/backend、跳过 GPU-only big-GPU harness、将 TRITON-only choice 扩为
  `TRITON,ATEN`，并把 GPU 正向 template 断言映射为产品 gate 负向断言。
- `product_gate_bypassed=false`、`disable_pad_mm=true`、`shape_padding=false`、目标 counter
  `0/0`、`triton_tem_fused_mm` 不存在；原始 mm eager/compiled correctness 通过。
- 成功轮不展开读取 `output_code.py`。

关键 SHA256：adapter `b2fa21a5700cb7aeef9f3083cc0c26e370a95c94961134da31dd01e459789189`；
result `fa716d6b20cedf2e56b5073b9f5612d964388657d55909bf160bd94e3e137459`；
generated code `24dda83026554c0cb41425328b006ac05566de998ad8b7f0952f1c687cc5385f`；
最终日志 `a9ba75cacb8be404c9a6380a92b237e54af1a3d0dcb008e3792f5e8ca7c6bdc7`。

## 初步判断

上游 GPU 的 pattern/template 正向合同有效；NPU 产品配置明确关闭 pad-mm，因此目标不出现是
`EXPECTED_DISABLED`，不是未知不支持。加入 ATEN 只保证 gate 关闭后的原始 mm 有合法运行路径，
不证明 pad replacement 已支持。

## 下游处理

作为 `AU-pad-mm-mm` 的 contract 辅助证据并入 unit result；不单独绑定新 variant，不进入 repair queue。

---

### Handoff

- **阶段结论**：`PASS`（产品状态 `EXPECTED_DISABLED`）。
- **下一步动作**：已并入 `AU-pad-mm-mm` 正式 comparison。
- **传递工件**：本报告、adapter、最终 run root。
- **关键发现摘要**：产品 gate 生效，目标 template 不出现，原始 mm correctness 有效。
- **阻塞条件**：无。
