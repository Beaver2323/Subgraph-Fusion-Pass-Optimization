# REF-pad-mm-original-aten-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-mm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：测试入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

社区方法 `PadMMTest.test_original_aten_preserved_pad_mm` 只由 GPU 测试 harness 生成，NPU 原生入口
为 0 tests。原意是检查 padding 候选存在时仍能保存原 ATen choice，而不是强迫 NPU 启用 padding。

## 2. 最小适配

```python
# issues/REF-pad-mm-original-aten-native/npu_adapter.py:48-73
class NpuOriginalAten(unittest.TestCase):
    @upstream.inductor_config.patch(
        force_shape_pad=True, max_autotune_gemm_backends="TRITON,ATEN"
    )
    def test_original_aten(self):
        compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

适配保留 fp16、原 shape、原 ATen fallback 和数值断言，只替换设备及入口；没有修改产品
`disable_pad_mm`。

## 3. 必要调用链

源码重建链：`adapter test → torch.compile → pad_mm choice construction → triton_experimental config
→ original ATen choice → correctness/gate assertions`。

## 4. 结果与边界

adapter 1/1 通过，ATen 原路径可运行而 pad rewrite 仍关闭；单元正式 verdict 为
`EXPECTED_PRODUCT_DIVERGENCE`。详细 run root、哈希和失败尝试见[复现报告](复现报告.md)。
