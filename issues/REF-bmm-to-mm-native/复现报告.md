# REF-bmm-to-mm-native NPU 复现报告

> 更新时间：2026-09-07 18:45 CST（UTC+08:00）
> 状态：社区功能合同 PASS；NPU 盈利性修复验证 PASS
> 后端：`triton_experimental`

## 1. 社区合同

社区入口：`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_bmm_to_mm`。冻结
PyTorch commit 为 `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。batch=1 必须从 bmm 改写为
`squeeze→mm→unsqueeze`，batch=3 必须保留 bmm；两路都比较 eager/compiled。

```python
# PyTorch: test/inductor/test_pattern_matcher.py:664
a = torch.randn(1, 16, 8, device=GPU_TYPE)
b = torch.randn(1, 8, 32, device=GPU_TYPE)
FileCheck().check("extern_kernels.mm(").check_not("extern_kernels.bmm(").run(code)

a_multi = torch.randn(3, 16, 8, device=GPU_TYPE)
b_multi = torch.randn(3, 8, 32, device=GPU_TYPE)
FileCheck().check("extern_kernels.bmm(").run(code_multi)
```

## 2. 原生复现

从 `/home/z50063656/tmp` 执行原模块。模块底部仅在 `IS_LINUX and HAS_GPU` 时调用
`run_tests()`；当前 NPU 进程中的上游 `HAS_GPU=false`，因此返回码虽为 0，实际执行 0 tests。
结论是 `NO_TESTS`，不是 PASS。

## 3. 最小适配与调用链

适配入口见 [`npu_adapter.py`](npu_adapter.py)，完整偏离说明见
[`适配报告.md`](适配报告.md)。必要源码链：社区方法 → `torch.compile` → joint graph
`bmm_to_mm` → `check_device(..., get_gpu_type())` → Inductor lowering/codegen → 原 FileCheck。

## 4. 动态结果

- 最小适配：1/1 tests PASS，0 failure/error/skip；batch=1 生成 `extern_kernels.mm`，batch=3 生成
  `extern_kernels.bmm`，证明 NPU 具备该改写能力。
- 性能：社区 shape 的 ON Event p50 回退 12.46%，三个 sensitivity shape 分别回退
  31.58%、18.79%、16.97%，结论 `PERF_REGRESSED`。
- 产品修复：默认 `disable_bmm_to_mm=true` 后 batch=1 保留 bmm；显式关闭门禁可恢复 mm。两臂均
  `triton_experimental`、fresh process、数值 PASS。
- 功能证据：`/home/z50063656/tmp/t079-npu-results/REF-bmm-to-mm-native/adapter-8723yeav/`。
- 修复证据：`/home/z50063656/tmp/t079-bmm-gate-results/bmm-gate-verify-20260907T184104+0800/`。

修复后的 NPU 默认结构与 CUDA 社区 FileCheck 有意不同，但计算合同不变；理由、源码差异与调用栈见
[`根因分析.md`](根因分析.md)和[`修复验证报告.md`](修复验证报告.md)。
