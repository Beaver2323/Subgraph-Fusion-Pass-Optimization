# REF-pad-mm-dynamic-m-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-mm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：产品关闭状态下的测试入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

`test_pad_mm.py::PadMMTest.test_pad_mm_dyn_m` 受 big-GPU/GPU-only harness 阻断；首轮照搬 CUDA 的
TRITON-only choice 又在 NPU 形成 `NoValidChoicesError`。这不是 pad pattern 的产品结论，详见
[复现报告](复现报告.md)。

## 2. 最小适配

```python
# issues/REF-pad-mm-dynamic-m-native/npu_adapter.py:56-59, 105
@upstream.inductor_config.patch(
    force_shape_pad=True, max_autotune_gemm_backends="TRITON,ATEN"
)
compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

只注入 NPU、保留社区 dynamic-M/shape/正确性并补合法 ATEN fallback；没有把
`disable_pad_mm=true` 改成 false。正例被映射为“产品 gate 下应保持未 padding”的观察合同。

## 3. 必要调用链

源码重建链：`npu_adapter.main → NpuPadMMDynamicM.test → torch.compile → pad_mm joint/post-grad
registration → triton_experimental loader 关闭 shape_padding → ATEN fallback → 数值与 gate 断言`。

## 4. 结果与边界

adapter 1/1 通过，动态 replay 正确且未生成 pad/slice，正式 verdict 为
`EXPECTED_PRODUCT_DIVERGENCE`。它证明关闭行为稳定，不证明 pad-mm 在 NPU 上已支持或值得启用。
