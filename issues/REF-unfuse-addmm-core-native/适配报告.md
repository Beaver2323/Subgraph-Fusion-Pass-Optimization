# REF-unfuse-addmm-core-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-unfuse-bias-add-to-pointwise`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：原社区方法实例化；`product_gate_bypassed=false`

## 1. 原生阻断

`TestPatternMatcher.test_unfuse_bias_addmm` 因 `HAS_GPU` 不识别 NPU 而 0 tests。原方法包含三个
子场景及 FileCheck，不能只复制一个数值函数，详见[复现报告](复现报告.md)。

## 2. 最小适配

```python
# issues/REF-unfuse-addmm-core-native/npu_adapter.py:45-51
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
suite = unittest.TestSuite([NpuCase("test_unfuse_bias_addmm")])
```

仅替换设备/入口；三个子场景、shape、torch.compile、数值和 FileCheck 均保持，产品配置不绕过。

## 3. 必要调用链

源码重建链：`NpuCase original method → torch.compile → post-grad unfuse_bias_addmm
→ pointwise bias path/NPU lowering → correctness/FileCheck`。

## 4. 结果与边界

adapter 进入完整原方法并形成正式 comparison；最终产品范围对该 addmm rewrite 显式关闭，单元为
`EXPECTED_PRODUCT_DIVERGENCE`、repair=`verified`。关闭原因与代码路径见[复现报告](复现报告.md)。
