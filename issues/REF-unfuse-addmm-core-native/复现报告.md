# REF-unfuse-addmm-core-native NPU 复现报告

> 更新时间：2026-09-06 07:59 CST（UTC+08:00）
> 阶段结论：`PASS`（三条社区结构/codegen 合同成立）
> NPU 后端：`triton_experimental`

## 基本信息

- **上游测试**：
  `test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_unfuse_bias_addmm`。
- **冻结 PyTorch**：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`；测试文件
  SHA256 为 `6a8a98d8f957f78ec99801b1cfd9d104bafb00cef9c2fbe59f3879c6340ea6fe`。
- **输入**：FP32 bias `[20]`、mat1 `[10,15]`、mat2 `[15,20]`。
- **GPU reference**：紧凑 handoff 中该 case 通过、FX before/after 已捕获；raw-text
  图正文待回传。

该方法一次执行三个子场景：

1. 单独 `addmm` 没有 pointwise consumer，生成代码必须保留 `extern_kernels.addmm`；
2. `gelu(addmm(...))`，应拆成 `mm + bias pointwise`，生成代码不得含 addmm；
3. `gelu(unsqueeze(addmm(...)))`，view 路径仍应拆分。

## 原生入口与适配边界

原文件末尾仅在 `HAS_GPU=true` 时启动测试；冻结 PyTorch 的 `HAS_GPU` 不统计 NPU，
故原生命令退出 0、执行 0 tests。原生日志：
`/home/z50063656/tmp/t078-npu-results/REF-unfuse-addmm-core-native/direct-w29gbvp6/`。

最小入口适配只设置 `GPU_TYPE=npu` 并显式构造原
`TestPatternMatcher.test_unfuse_bias_addmm`。三个函数、shape、`torch.compile` 调用和
原 `FileCheck` 均未修改，也没有改 upstream `is_gpu` guard。运行态结果表明当前注册后的
Inductor `is_gpu` 能识别 NPU，因此无需产品 capability 修复。

## 执行命令

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
export ASCEND_RT_VISIBLE_DEVICES=0

python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/issues/REF-unfuse-addmm-core-native/run_native.py
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/issues/REF-unfuse-addmm-core-native/run_native.py --adapter
```

## 执行结果

```text
Ran 1 test in 52.364s
OK
```

有效日志：
`/home/z50063656/tmp/t078-npu-results/REF-unfuse-addmm-core-native/adapter-x6wlyqin/`。

| 子场景 | transformed FX | generated code | 原断言 |
|---|---|---|---|
| no pointwise | `aten.addmm` | `extern_kernels.addmm` | PASS |
| GELU | `aten.mm → aten.add → GELU ops` | `extern_kernels.mm` + Triton pointwise | PASS |
| unsqueeze + GELU | `aten.mm → aten.add → unsqueeze → GELU ops` | `extern_kernels.mm` + Triton pointwise | PASS |

GELU 分支的核心 transformed FX：

```python
# test/inductor/test_pattern_matcher.py:1503
mm_default = torch.ops.aten.mm.default(arg0_1, arg1_1)
add_tensor = torch.ops.aten.add.Tensor(arg2_1, mm_default)
# 后续 GELU pointwise
```

生成代码先执行 `extern_kernels.mm(...)`，随后把 bias add 与 GELU 放入
`triton_unk_fused_addmm_gelu_0`，符合 pass 的意图。

## 证据范围

- 社区方法真实编译并执行三个子场景，0 skip、无 CPU fallback。
- 原社区方法只检查 generated code，不单独把输出与 eager 比较；因此本报告把它记为
  **结构/codegen PASS**，不扩大成独立数值精度结论。
- 同 acceptance unit 的 leaf、expanded、computed-accumulator 等后续社区方法包含
  compiled/eager 数值断言，将单独执行，不能由本 case 代替。
- 本 case 不需要 torch_npu 源码变更。性能尚未测量。

---

### Handoff

- **阶段结论**：PASS（结构/codegen）
- **目标行为**：no-consumer 保留 addmm；两个 pointwise consumer 均拆为 mm+pointwise
- **下一步动作**：执行 computed-accumulator 负例，补充独立数值与 guard 证据
- **传递工件**：`run_native.py`、`npu_adapter.py`、三组 FX/IR/output_code
- **阻塞条件**：无；GPU raw-text 图正文待补
