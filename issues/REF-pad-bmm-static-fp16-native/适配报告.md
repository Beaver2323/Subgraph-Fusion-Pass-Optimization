# REF-pad-bmm-static-fp16-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-bmm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：静态 fp16 测试入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

社区 `PadMMTest.test_pad_batch` 属于 GPU-only harness。适配必须保留 batch=3、K=11 等不对齐形状和
fp16，不能换成更容易执行的 fp32/aligned case。

## 2. 最小适配

```python
# issues/REF-pad-bmm-static-fp16-native/npu_adapter.py:44-52
@upstream.inductor_config.patch(
    force_shape_pad=True, max_autotune_gemm_backends="TRITON,ATEN"
)
def test_static_fp16(self):
    compiled = torch.compile(torch.bmm, options={"npu_backend": "triton_experimental"})
```

设备和入口改变；原 shape/dtype/correctness 保持。ATEN 只用于避免 TRITON-only 无 choice，产品 gate
保持 `disable_pad_mm=true`。

## 3. 必要调用链

源码重建链：`adapter test → torch.compile(torch.bmm) → pad_bmm gate → NPU lowering choice
→ correctness 与 no-padding assertion`。

## 4. 结果与边界

adapter 1/1 通过，静态 fp16 原计算正确但未发生 padding；正式 verdict 为
`EXPECTED_PRODUCT_DIVERGENCE`，详见[复现报告](复现报告.md)。
