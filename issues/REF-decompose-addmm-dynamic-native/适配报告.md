# REF-decompose-addmm-dynamic-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-077｜`AU-decompose-mem-bound-mm-decompose-addmm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：dynamic addmm 社区 shape 镜像；`product_gate_bypassed=false`

## 1. 原生阻断

`TestDecomposeMemMM.test_dynamic_shape_decompose_addmm` 在 NPU 原生文件执行中 skip。社区意图是验证
large-M/small-KN dynamic addmm 的分解，不允许改成静态 mm 或移除 bias。

## 2. 最小适配

```python
# issues/REF-decompose-addmm-dynamic-native/npu_adapter.py:36-46
left = torch.randn(m, k, device="npu")
right = torch.randn(k, n, device="npu")
bias = torch.randn(n, device="npu")
compiled = torch.compile(fn, dynamic=True,
    options={"npu_backend": "triton_experimental"})
```

只镜像冻结 shape、bias、dynamic 和 target count；NPU device guard 未修改。

## 3. 必要调用链

源码重建链：`adapter.main → dynamic addmm body → torch.compile → decompose_addmm handler
→ device guard → original addmm lowering → replay correctness/counter`。

## 4. 结果与边界

动态执行正确但目标分解为 0，正式 verdict 为 `EXPECTED_PRODUCT_DIVERGENCE`。测试态最小候选性能
显著回退，因此保留 guard；来源与数值见 [T-077 pattern 导读](../../report/t077_pattern_gpu_npu_guide_20260902.md)。
