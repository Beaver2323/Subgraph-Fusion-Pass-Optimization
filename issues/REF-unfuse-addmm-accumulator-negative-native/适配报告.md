# REF-unfuse-addmm-accumulator-negative-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-unfuse-bias-add-to-pointwise`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：accumulator 负例原方法实例化；`product_gate_bypassed=false`

## 1. 原生阻断

`test_preserve_accumulator_addmm_with_pointwise` 受 `HAS_GPU` 门阻断。它要求有额外 pointwise 用户的
accumulator 保持融合，是防止 unfuse 扩大匹配域的负例。

## 2. 最小适配

```python
# issues/REF-unfuse-addmm-accumulator-negative-native/npu_adapter.py:44-50
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
suite = unittest.TestSuite([NpuCase(METHOD)])
```

只实例化原方法；accumulator 使用关系、pointwise、shape、FileCheck 与负向预期均不改。

## 3. 必要调用链

源码重建链：`original accumulator method → torch.compile → unfuse candidate guard rejects
→ fused addmm preserved → correctness/FileCheck`。

## 4. 结果与边界

负例通过并保持 addmm，防止条件修复错误扩大作用域；单元整体正式为
`EXPECTED_PRODUCT_DIVERGENCE`。原生/adapter 证据见[复现报告](复现报告.md)。
