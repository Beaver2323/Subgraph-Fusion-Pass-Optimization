# REF-unfuse-addmm-accumulator-negative-native NPU 复现报告

> 更新时间：2026-09-06 08:03 CST（UTC+08:00）
> 阶段结论：`PASS`（computed accumulator 正确保留 addmm）
> NPU 后端：`triton_experimental`

## 测试合同

上游方法：
`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_preserve_accumulator_addmm_with_pointwise`，
冻结 PyTorch commit 为 `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。

输入 accumulator 是 FP32 `[10,20]`，先经过 `torch.sin`，再进入
`gelu(addmm(sin(inp), [10,15], [15,20]))`。虽然 addmm 有 pointwise consumer，
`sin(inp)` 不是 placeholder/get_attr/broadcast/stride-0 bias-like leaf，因此 pass 必须拒绝拆分：

- compiled 与 eager 数值一致；
- generated code 保留 `extern_kernels.addmm`。

GPU 紧凑 reference 中该 case 通过且 FX before/after 签名相同。

## 原生入口与适配

原生入口受上游 `HAS_GPU`（不统计 NPU）启动门阻断，退出 0 但 0 tests；日志为
`direct-99o1_gr1/`。最小适配仅做 `GPU_TYPE=npu` 和显式运行原方法，未修改
`sin`、shape、数值断言或 `FileCheck`。

## 执行命令

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
export ASCEND_RT_VISIBLE_DEVICES=0
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/issues/REF-unfuse-addmm-accumulator-negative-native/run_native.py --adapter
```

## 结果与图证据

```text
Ran 1 test in 36.306s
OK
```

有效工件：
`/home/z50063656/tmp/t078-npu-results/REF-unfuse-addmm-accumulator-negative-native/adapter-5asmc8jw/`。

transformed FX 保留：

```python
# test/inductor/test_pattern_matcher.py:1587-1588
sin = torch.ops.aten.sin.default(arg2_1)
addmm = torch.ops.aten.addmm.default(sin, arg0_1, arg1_1)
# 后续 GELU pointwise
```

output code 先生成 sin Triton kernel，再调用：

```python
extern_kernels.addmm(buf0, arg0_1, arg1_1, alpha=1, beta=1, out=buf1)
```

原数值断言和 codegen 断言均通过，0 skip，无 CPU fallback。本 case 不需要源码修复，
也不参与性能统计。

---

### Handoff

- **阶段结论**：PASS
- **目标 guard**：computed accumulator 不拆分，addmm 保留
- **下一步动作**：执行 same-shape leaf 正例
- **传递工件**：`run_native.py`、`npu_adapter.py`、FX/IR/output_code
- **阻塞条件**：无
