# REF-mm-plus-mm-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-post-grad-mm-plus-mm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：测试入口适配，不是产品修复；`product_gate_bypassed=false`

## 1. 原生阻断

社区入口 `test_pattern_matcher.py::TestPatternMatcher.test_mm_plus_mm` 在 NPU 上因 GPU-only 测试
发现条件执行 0 tests。GPU 已证明同一方法覆盖 same-K、different-K fallback 和形状负例；因此不能
另造更容易命中的图。完整原始日志与结果见同目录[复现报告](复现报告.md)。

## 2. 最小适配

```python
# issues/REF-mm-plus-mm-native/npu_adapter.py:50-80
torch.npu.set_device("npu:0")
upstream.GPU_TYPE = "npu"
compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

偏差仅为 NPU device/backend、显式实例化原合同及 FX/counter 采集；shape、dtype、两个 operand
顺序、same/different-K 和负例均保持。adapter 运行 4 个目标分支，不修改上游 pattern 或产品 gate。

## 3. 必要调用链

以下为源码与 artifact 重建链，不是异常 traceback：
`npu_adapter.main → upstream test body/function → torch.compile → post_grad_passes → mm_plus_mm handler → triton_experimental lowering/codegen → eager/compiled 与 counter 断言`。

## 4. 结果与边界

适配后 4/4 分支正确，same-K 命中、different-K 与形状不匹配安全保留，正式 comparison 为
`BEHAVIOR_UNCHANGED`。该结论证明社区合同可进入 NPU，不证明所有 shape 都有性能收益；性能结论仍以
`results/current/T-076/performance_summary.json` 为准。
