# T-103 pattern 8 安装态基线复核

> 证据时间：2026-09-14T22:23:40.221476+08:00；复核时间：2026-09-14T22:26:28+08:00。

## 1. 本轮结论与适配范围

原方法 tests=1/skip=0；已通过Tensor比较=1；本编号精确改写=0；原合同未通过；后续分支不计覆盖。

GPU已确认本编号；NPU固定triton_experimental。原生类定义阻断已有原件，随后执行本case适配。
保留原方法、dtype/shape/训练推理参数和数值容差；CUDA代码符号断言显式适配为实际NPU调用。
未改安装态产品、未加载注册候选、未签性能门禁。详见[适配报告](适配报告.md)。

## 2. 原代码合同与必要调用链

```text
test/inductor/test_fused_attention.py::SDPAPatternRewriterGpuTests.test_sdpa_rewriter_8_gpu
 -> _check_common -> torch.compile -> AOT joint_graph -> fuse_attention pattern
 -> backend lowering/codegen -> NPU -> 原counter/codegen/数值断言
```

```python
# 冻结 test/inductor/test_fused_attention.py:121、137、140（节选）
self.assertGreaterEqual(counters["inductor"]["fuse_attention"], 1)
if not has_dropout or override_check_equal:
    self.assertEqual(result1, result2, atol=atol, rtol=rtol)
# training分支执行backward；相同条件成立时才比较浮点输入梯度。
```

数值比较为零时不能记精度通过；未执行的后续参数分支不借GPU结果补成NPU通过。

## 3. 实际目标与生成代码线索

实际发生图变化的注册：`_sfdp_pattern_7_half_inference`。

实际debug生成图数量：2。下表是AST读取的调用，不是性能测量或完整fallback判决。

| 生成调用 | 图内静态出现次数 |
|---|---:|
| `torch.ops.npu.npu_fusion_attention_v3.default` | 1 |
| `triton_unk_fused_matmul_permute_0.run` | 2 |
| `triton_unk_fused_matmul_permute_transpose_1.run` | 1 |
| `extern_kernels.bmm` | 2 |
| `triton_unk_fused__to_copy_all_div_isfinite_matmul_2.run` | 1 |
| `torch.ops.aten.any.dims` | 1 |
| `triton_unk_fused__softmax__to_copy_all_amax_div_matmul_mul_sub_3.run` | 1 |

可疑CPU行数量：0；无可疑字符串不自动等于无fallback。

## 4. 原失败栈与后续工作

```text
Traceback (most recent call last):
  File "/home/z50063656/Pass/src/pytorch/torch/testing/_internal/common_utils.py", line 3829, in wrapper
    method(*args, **kwargs)
  File "/home/z50063656/Pass/src/pytorch/test/inductor/test_fused_attention.py", line 556, in _test_sdpa_rewriter_8
    self._check_common(sfdp_pattern_8, args, atol=2e-3)
  File "/home/z50063656/Pass/src/pytorch/test/inductor/test_fused_attention.py", line 121, in _check_common
    self.assertGreaterEqual(counters["inductor"]["fuse_attention"], 1)
AssertionError: 0 not greater than or equal to 1

To execute this test, run the following from the base repo dir:
    python attention_community_npu.py NpuCase.test_sdpa_rewriter_8_gpu

This message can be suppressed by setting PYTORCH_PRINT_REPRO_ON_FAILURE=0

```

以上为真实失败栈。根因未在本生成报告中自动下结论；不能仅因相同counter症状就套用其他编号修复。

[本轮原件](evidence/t103-native-wv7gd85d/adapter/)包含 result、执行器快照、目标FX、前后IR和output_code。

[性能测例来源与数值边界](../../report/attention_performance_contract_review_20260914.md)。
