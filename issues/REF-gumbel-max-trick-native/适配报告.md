# REF-gumbel-max-trick-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-077｜`AU-apply-gumbel-max-trick`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：GPU 测试生成与统计合同适配；`product_gate_bypassed=false`

## 1. 原生阻断

社区 `TestPatternMatcherLogging.test_gumbel_max_trick` 没有自然生成 NPU test body。该优化针对随机
采样分布，不能用一次逐元素 equality 代替百万样本统计合同。

## 2. 最小适配

```python
# issues/REF-gumbel-max-trick-native/npu_adapter.py:43-50
logits = torch.arange(1, categories + 1, device="npu").log()
with torch._inductor.config.patch(apply_gumbel_max_trick=True):
    compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

适配只映射 NPU/backend 并复用社区类别概率、样本规模、统计容差与 target counter；算法和阈值不改。

## 3. 必要调用链

源码重建链：`adapter.main → community-equivalent sampling body → torch.compile
→ apply_gumbel_max_trick pattern → triton_experimental codegen → distribution/counter assertions`。

## 4. 结果与边界

六轮百万样本统计及 target counter 均通过；同 backend OFF/ON 性能也通过，正式 verdict 为
`PERF_IMPROVED`。详细 GPU/NPU 对照与性能数值见
[T-077 汇总](../../report/t077_npu_completion_20260902.md)。
