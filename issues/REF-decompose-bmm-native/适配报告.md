# REF-decompose-bmm-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-077｜`AU-decompose-mem-bound-mm-decompose-bmm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：社区阈值变体镜像；`product_gate_bypassed=false`

## 1. 原生阻断

`TestDecomposeMemMM.test_decompose_bmm` 所在社区文件在 NPU 原生执行中整体 skip。合同包含 large
batch/small MNK 正例、other-dims threshold 和 batch threshold 两个负例。

## 2. 最小适配

```python
# issues/REF-decompose-bmm-native/npu_adapter.py:42-56
with torch._inductor.config.patch(post_grad_fusion_options=options):
    left = torch.randn(batch, m, k, device="npu")
    right = torch.randn(batch, k, n, device="npu")
    compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

只镜像冻结社区三组 shape/threshold 和 counter，设备改为 NPU；不修改上游 NPU device guard。

## 3. 必要调用链

源码重建链：`adapter.main → three bmm variants → post_grad decompose_mem_bound_mm
→ device guard → original bmm lowering → correctness/counter assertions`。

## 4. 结果与边界

三变体数值正确，正例未分解、负例保持未分解；正式 verdict 为
`EXPECTED_PRODUCT_DIVERGENCE`。测试态绕过 guard 的候选性能回退，故 guard 保留，详见
[pattern 导读](../../report/t077_pattern_gpu_npu_guide_20260902.md)。
