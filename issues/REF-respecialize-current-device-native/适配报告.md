# T-087 运行时设备解析最小适配

> 更新时间：2026-09-11 15:27 CST（UTC+08:00）
> 状态：原生SKIP、安装态wrapper失败及两级源码候选均已实测；最终候选1/1通过，非安装态已修复。

## 1. 原生阻断

原生 `test_inductor_compiles_under_coor` 因 `requires CUDA` 跳过，tests=1、skip=1，不算 PASS。源码及原始日志见本目录 `native_runs/`。未发现 NPU 明确产品 disable。

## 2. 最小适配

```python
# issues/REF-respecialize-current-device-native/npu_adapter.py
# 从冻结 test_compile_on_one_rank.py 抽取当前方法及两个原helper。
# 只将设备API、设备字面量和对应无固化设备的正则 cuda -> npu。
compiled = torch.compile(self._coor_inductor_fn, backend='inductor', fullgraph=True)
out, codes = run_and_get_code(compiled, torch.randn(2, 8, device='npu'))
self.assertEqual(out.device.type, 'npu')
FileCheck().check('torch.npu.current_device()').run('\n'.join(codes))
self._assert_no_baked_device('\n'.join(codes))
```

原数学函数、compile-on-one-rank 开关、current_device 及三种无固化 index 检查均保留。另补同输入 eager 数值比较；生成代码在断言之前保存，失败也可审阅。`product_gate_bypassed=false`；不修改冻结 PyTorch 源码。

## 3. 必要调用链

```text
原_coor_inductor_fn -> torch.compile(fullgraph=True, compile_on_one_rank=True)
 -> proxy_tensor: coor.current_device -> post_grad.respecialize_current_device_nodes
 -> lowering -> wrapper/kernel codegen -> NPU执行 -> 原device/FileCheck/regex断言
```

## 4. 结果与边界

实际运行固定 `triton_experimental`，导入前选后端、子进程中核验实际注册后端。
安装态在目标改图后发生wrapper异常；两文件隔离候选执行真实NPU sum与zeros/add，数值/eager及原codegen检查全部通过。
源码候选通过不等于已安装支持；单设备测例不证明多rank复用；正确性必需改写无合法OFF，性能免测。
两级实际错误、最小产品diff及修复前后编译产物见 [根因分析](根因分析.md)。
