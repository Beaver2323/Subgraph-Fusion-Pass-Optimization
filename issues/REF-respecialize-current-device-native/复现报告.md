# T-087 运行时设备解析 NPU 复现

> 开始时间：2026-09-11（Asia/Shanghai；精确运行时间由逐次日志记录）
> 更新：2026-09-11 18:44 CST（UTC+08:00）；Pass安装态已修复，原例1/1与四项近邻通过。
> 下文保留15:27的复现/候选阶段过程；当前验证与代码/日志见[修复验证报告](修复验证报告.md)。

## 基本信息

原生入口：`test/distributed/tensor/test_compile_on_one_rank.py::TestCompileOnOneRankDeviceAsParameter.test_inductor_compiles_under_coor`。
冻结 PyTorch commit：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。
意图：compile-on-one-rank 模式生成真实设备 kernel，设备由运行时 current_device 解析，不固化 rank 特定 device index。
本例只使用单设备；不把它外推为多 rank 或跨卡复用验证。关键依赖为 factory、sum、Inductor wrapper、设备上下文。

## 环境信息

已核验：Pass Python 3.11.15，torch 2.14.0a0+git8e86e0a（editable冻结源码），torch_npu 2.14.0a0+git83cc452（Pass site-packages），CANN 9.0.1，8张910B2；本次物理卡2无运行进程。实际注册后端为triton_experimental。

## 原生预检与禁用列表

当前 torch_npu 源码没有 `test_upsteam/disabled_testcases.json` 或对应 patch。
已检查安装态 experimental 相关目录，未发现目标明确 disable。原生方法包含 CUDA 可用性 skip；先直接执行并留证。

## 复现命令

从 `/home/z50063656/tmp` 启动 `scripts/run_prepared_npu_case.py --task T-087 --case REF-respecialize-current-device-native --npu 2`。
后端必须在导入前选择 `triton_experimental`。卡号最终按本次空闲检查调整。

## 关键环境变量

CONDA_PREFIX=/home/z50063656/envs/Pass；ASCEND_HOME_PATH=/usr/local/Ascend/cann9.0.1/cann-9.0.1；PYTHONPATH仅CANN python/tbe目录；ASCEND_RT_VISIBLE_DEVICES=2、SET_NPU_DEVICE=0。禁止从 torch_npu 源码目录导入产品包。

## 执行结果

2026-09-11T14:23:16+08:00 原生执行：return_code=0、tests=1、skip=1、原因requires CUDA。记录在native_runs/native-20260911T142316-zjle1lbf。
14:25设备适配后真实wrapper接口错误；15:03第一版候选数值通过但原无固化index断言失败；15:09两文件候选1/1原合同通过。候选使用空闲物理卡5。
完整原命令、错误栈、FX/IR/output_code及哈希见 [根因分析](根因分析.md)。未覆盖旧记录、未放宽断言。

## 初步判断与修复方向

先区分 CUDA harness 阻断和 NPU 实际 wrapper 能力差异；如果需要设备适配，保留原数学函数、codegen 约束及全部断言。

## 下游处理与 Handoff

无合法 OFF 的必需 lowering 前改写，性能免测。安装态未改，源码候选通过原合同不等于正式产品修复；待安装态同步授权/部署回归，多设备复用另验。
