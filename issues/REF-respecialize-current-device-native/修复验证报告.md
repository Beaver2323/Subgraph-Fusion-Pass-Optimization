# T-087 设备解析修复验证报告

> 更新时间：2026-09-11 18:58 CST（UTC+08:00）；安装态原例1/1、近邻4/4、两项修复共同安装后原例再验1/1通过。

## 问题描述

原社区 CooR 编译合同在 NPU 因缺 current_device_idx_expr 失败；只补该接口又被无固化 DeviceProperties 编号断言拦截。详见 [根因分析](根因分析.md)。

## 修复内容摘要

仅修改 Pass 安装态 experimental/device.py 和 experimental/codegen/triton.py，已备份；不改其他后端、系统环境或冻结 PyTorch。源码候选在独立 torch_npu 工作树，Python 变更不需要编译 C++。

## 验证结果

### 原用例回归

| 用例 | 修复前 | 修复后 | 核验 |
| --- | --- | --- | --- |
| test_inductor_compiles_under_coor | FAIL：wrapper 设备接口缺失 | PASS，1 test / 0 skip | 精确目标调用并改图、设备数值一致、原无固化编号断言通过 |

### 近邻用例验证

| 近邻 | 结果 | 检查范围 |
| --- | --- | --- |
| ordinary-compile | PASS | 原 factory/reduction 图在 CooR=False 下的 FP32/FP16 数值与正常 index=0 metadata |
| test_cpp_wrapper_under_coor_rejected | PASS | 原有 CooR + cpp_wrapper 拒绝保持 |
| test_fx_wrapper_under_coor_rejected | PASS | 原有 CooR + fx_wrapper 拒绝保持 |
| test_inductor_code_identical_across_devices | PASS | 物理 NPU 5/6，benchmark_kernel 与 compile-time autotune 两配置下代码一致、无固化设备号 |

普通编译为社区函数派生近邻，其他三项保留原社区方法，仅 cuda→npu。共 4 个独立进程，全部 1 test / 0 skip；双卡方法内部执行四次编译。不扩大原单设备 GPU 分母。

### 编译验证（如适用）

不执行 C++ 构建：仅 Python 修改。真实 kernel 编译由测试覆盖。

### 联合安装态回归

18:55在T-096注册入口/配置/新模块也已安装的最终Pass环境再跑本原例，仍1 test / 0 skip / exit0、数值一致且目标改图，runtime设备表达式和index=None原断言通过。
[联合安装审查及5文件哈希](combined_install_review_20260911.json) / [完整日志](adapter_runs/native-20260911T185556-nyi85iua/) / [FX、IR、生成代码](evidence/combined-installed-20260911T185556/)。这是同合同回归，不新增分母。

## 代码变更审查

候选差异：[current_device_and_codegen.patch](current_device_and_codegen.patch)。已安装文件包含已有修复，仅应用本次两处语义改动，不整文件覆盖。

```diff
# torch_npu/_inductor/triton_experimental/device.py
+    def current_device_idx_expr(self) -> str:
+        return "torch.npu.current_device()"

# torch_npu/_inductor/triton_experimental/codegen/triton.py
+def _npu_codegen_device():
+    from torch.fx.experimental.proxy_tensor import _coor_enabled
+    device = V.graph.get_current_device_or_throw()
+    return torch.device(device.type) if _coor_enabled() else device
-            "device": DeviceProperties.create(V.graph.get_current_device_or_throw()),
+            "device": DeviceProperties.create(_npu_codegen_device()),
```

这不是将固定编号隐藏到变量：CooR 的 None 是上游 runtime 规定的设备延迟解析 sentinel。普通编译仍使用原设备，其他后端类未改。

## 验证命令与关键输出

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/run_prepared_npu_case.py --task T-087 --case REF-respecialize-current-device-native --npu 5 --adapter
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/verify_t087_installed_neighbors.py --devices 5,6
```

原例实际命令没有 `--candidate-device`；结果 `product_candidate=null`，导入路径为 Pass site-packages。启动器记录执行前后安装文件 SHA256 完全一致，与[部署/备份记录](installed_deployment_20260911.json)绑定。
原例完整输出：[stderr](adapter_runs/native-20260911T181204-4oswvqij/stderr.log)；[近邻逐命令、设备、PID](installed_neighbor_runs/20260911T181410-k_inh39q/execution.json)及同目录逐例日志。

```text
原例：tests_ran=1 tests_skipped=0 return_code=0
END ordinary-compile rc=0
END cpp-wrapper-rejected rc=0
END fx-wrapper-rejected rc=0
END two-device-code-identical rc=0
```

```python
# 实测 evidence/installed-pass-20260911T181204/adapter/output_code_0.py:178
_coor_device_idx = torch.npu.current_device()
# 同文件 triton_meta：DeviceProperties(type='npu', index=None, ...)
triton_unk_fused_sum_0.run(arg0_1, buf0, 1, 16, stream=raw_stream)
triton_unk_fused_add_zeros_1.run(buf0, buf1, 32, stream=raw_stream)
```

[修复前](evidence/before-current-device-fix-20260911/) / [安装态修复后 FX、IR、output_code](evidence/installed-pass-20260911T181204/)均入库，哈希见各自 inventory。日志里的“未编译 torchair”提示是另一个 Dynamo 后端的注册通知，不是本次 Inductor 执行退回 eager；实际 kernel 与数值证据单独核验。

## 残余风险

已覆盖当前单设备合同及两设备独立生成代码的一致性；未验证多 rank 通信、跨进程二进制传输或跨卡复用同一个 kernel handle。没有合法 OFF 可供性能比较，性能按必需正确性改写免测，不声称速度提升。仅 Pass 已部署，未提交社区 PR 或发布 wheel。

### Handoff

- 阶段结论：全部通过；T-087 两个单元正式完成。
- 下一步动作：源码差异归档，保留明确的单设备/双设备近邻边界。
- 传递工件：源码 patch、备份、修复验证报告。
- 验证覆盖：C++ 编译否；原用例 PASS；近邻 4 项 PASS。
- 残余风险等级：minor（多 rank 不在本合同内）。
- 阻塞条件：无当前合同阻塞；社区合入未执行。
