# REF-pad-bmm-dynamic-batch-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-bmm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：动态 batch 测试入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

`PadMMTest.test_pad_bmm_dyn_b` 受 GPU-only big-GPU harness 阻断，无法自然生成 NPU test body。合同要求
同一 compiled callable 处理动态 batch，而不是分别编译两个静态样本。

## 2. 最小适配

```python
# issues/REF-pad-bmm-dynamic-batch-native/npu_adapter.py:44-61
@upstream.inductor_config.patch(
    max_autotune=True, max_autotune_gemm_backends="TRITON,ATEN", force_shape_pad=True
)
def test_dynamic_batch(self):
    compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

只适配 device/backend、入口和合法 fallback；动态 batch、B/M/K/N、dtype 与 replay correctness 保持，
不解除 pad-bmm 产品 gate。

## 3. 必要调用链

源码重建链：`adapter test → dynamic torch.compile → pad_bmm registration/gate → ATEN NPU path
→ first/replay correctness 与 target counter`。

## 4. 结果与边界

adapter 1/1 通过、动态执行正确，padding 仍明确关闭；正式 verdict 为
`EXPECTED_PRODUCT_DIVERGENCE`。证据摘要和 SHA256 见[复现报告](复现报告.md)。
