# 原生 NPU 测试入口复现

> 时间：2026-09-11T12:11:50.417678+08:00

## 用例

T-088 / REF-split-cat-aten-singular-native

## 命令

```text
/home/z50063656/envs/Pass/bin/python /home/z50063656/Pass/src/pytorch/test/inductor/test_split_cat_fx_aten_passes.py -v TestSplitCatAten.test_split_cat_post_grad_singular
```

## 环境

Pass 环境；后端在导入前选择 triton_experimental；cwd 为临时工作目录。

## 结果

执行中，未判定 PASS。

## 下游处理

等待日志与合同复核。

## 执行完成

状态 `skipped-or-xfail`；返回码 0；tests=1；skip=1。

完整输出见同目录 stdout.log / stderr.log。原生入口结果不等于 NPU 合同/性能通过。
