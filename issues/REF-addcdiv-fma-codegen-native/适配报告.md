# REF-addcdiv-fma-codegen-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-fuse-addcdiv-to-fma`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：原方法绑定与只读 codegen 观察；`product_gate_bypassed=false`

## 1. 原生阻断

`CommonTemplate.test_addcdiv_fma_uses_fma_and_div_rn` 缺少 NPU `GPUTests` 实例，且 CUDA 基类额外
打开 runtime Triton static assert。原生预检未进入测试体，详见[复现报告](复现报告.md)。

## 2. 最小适配

```python
# issues/REF-addcdiv-fma-codegen-native/npu_adapter.py:44-63
upstream.GPU_TYPE = "npu"
method = inspect.unwrap(upstream.CommonTemplate.test_addcdiv_fma_uses_fma_and_div_rn)
class NpuCase(upstream.TestCase):
    device = "npu"
    test_addcdiv_codegen = method
```

仅去除 CUDA 测试包装、关闭 CUDA-only 诊断插桩并旁路记录原 counter/code-contains；`tl.fma`、
`div_rn`、shape/dtype 和断言判据不改。

## 3. 必要调用链

源码重建链：`NpuCase.test_addcdiv_codegen → original CommonTemplate method → torch.compile
→ post-grad FMA rewrite → NPU codegen → counter/tl.fma/div_rn assertions`。

## 4. 结果与边界

适配暴露 device guard/代码生成差异，条件修复后社区断言通过；正式 verdict 为 `PERF_NEUTRAL`，
repair=`verified`。产品改动与邻近回归见[根因分析](根因分析.md)和[修复验证报告](修复验证报告.md)。

## 5. 2026-09-08 低精度适配补充

早期入口适配只绑定社区 FP32 方法；本轮低精度 GPU case 是“社区合同仅改 dtype”的 tracker 派生
测例，不伪装成社区原生。NPU 侧不再增加测试包装，而使用
`verify_lowp_three_arm.py → torch.compile → post-grad pattern → exact addcdiv lowering → Triton`
六个 fresh process 直接观察产品链路。

三臂证明 BF16 可进入相同 pattern/lowering，因而产品最小适配只将
历史 BF16 阶段曾将 `supported_dtypes` 扩为 `(torch.float32, torch.bfloat16)`。FP16 后续已完成
后端专属显式舍入 lowering；`value=1` 补充 pattern 已在合同复核后移除，详见
[FP16 精度修复报告](FP16精度修复报告.md)。适配没有改变 shape、
value、位级断言或 FMA/div_rn 判据，也没有添加 CPU fallback/skip。完整代码框、调用栈和原始工件见
[低精度三臂与 BF16 修复报告](低精度三臂与BF16修复报告.md)。
