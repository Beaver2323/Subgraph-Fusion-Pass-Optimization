# fix(inductor): support dtype-accurate addcdiv re-fusion on NPU Triton experimental

> 更新时间：2026-09-09 18:05:53 CST（UTC+08:00）

# 【合入来源】

- [ ] 需求
- [ ] 问题单
- [x] issue/工单：T-078 / `REF-addcdiv-fma-codegen-native`
- [ ] 重构优化
- [ ] 资料更新

# 【修改方案】

1. 在 `triton_experimental` 激活时，向 post-grad 第 2 轮注册 NPU 专用的
   `div → mul → add` 到 `aten.addcdiv` 重融合 pattern。
2. pattern guard 接受已在 910B2 动态验证的 FP32/BF16/FP16 `self/t1/t2/output` 和 NPU
   device。FP16 只接受具体 Python scalar；tensor-valued scalar 与非浮点 `self` 保持不命中。
3. experimental 的统一 fallback 扫描完成后，窄注册
   `aten.addcdiv.default` lowering。FP32/BF16 使用 `ops.div_rn` 和 `ops.fma`；FP16 在单 kernel
   内显式保留除法、乘法后的低精度舍入。
4. `value=1` 在 decomposition 中消去乘一后不属于该 FMA pattern；保持 `add(div)`，要求
   bitwise 正确且 `addcdiv_fma_fused=0`，不注册后端补充重融合。为使该普通
   `div -> add` 路径与 NPU eager 位级一致，仅在 NPU FP16 `aten.div.Tensor`
   lowering 中恢复 quotient 的 FP16 downcast/upcast 边界。
5. 新增 `test_triton_experimental_addcdiv_precision.py`，锁定 value=1 的 bitwise、counter=0、
   一次 FP16 round 与无 FMA/div_rn 合同。
6. 保持冻结 PyTorch 不变，不增加 CPU fallback，不跳过社区断言，也不把整个
   `addcdiv` packet 加入通用生成白名单。

# 【资料变更】

不涉及客户资料变更。tracker 内补充了复现、根因、修复验证和本合入说明，用于审计。

# 【接口变更】

不涉及客户可见 Python/C++ API 变更；只补齐 `triton_experimental` 内部 FX pass
与 lowering 注册。

# 【功能验证】

验证环境：PyTorch `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`、
torch_npu `83cc452480c3546fd5cccf853bfe3a360ce9dbfc` 工作树、
Triton Ascend 3.2.0、CANN 9.0.1、Ascend 910B2、后端
`triton_experimental`。每个 OFF/ON/近邻场景均使用 fresh process。

| 场景 | 结果 |
|---|---|
| 原社区 codegen：counter + `tl.fma` + `div_rn` | PASS |
| 派生 FP32 `value=2.0` 位级正确性 | PASS，最大误差 0 |
| 社区 FP32 `value=1.0/2.0` 位级 case | PASS |
| BF16 扩展 | PASS，位级一致、融合计数 1、FMA/div_rn |
| FP16 `value=0.3/2/7.7` | PASS，位级一致、融合计数均为 1、显式低精度舍入 |
| `value=1` bitwise 邻接 | PASS；预期不命中，融合计数 0，mismatch=0，最大误差=0 |
| 非浮点 self / tensor-valued value guard | PASS，融合计数均为 0 |
| `py_compile` / `git diff --check` | PASS |

正式源码不是通过复制到 site-packages 验证，而是由 Python 启动期 finder 只定向加载
三个修改模块；结构化结果逐项校验其 `__file__` 均来自当前 torch_npu 工作树。

# 【影响范围】

- 仅影响选择 `triton_experimental` 后端且满足 FP32/BF16/FP16 addcdiv pattern 的图。
- 不含 C/C++ 修改，无需重编译 torch_npu 二进制。
- FP16 进入后端专属精确路径；不使用 FMA/div_rn，避免改变 NPU eager 舍入语义。
- FP32/BF16 三轮性能均为 `PERF_NEUTRAL`。FP16 修复前 OFF 不正确，暂不声明 pass 收益。

# 【CheckList】

- [x] 代码注释完备，关键注册顺序与三种 dtype 的不同 codegen 合同已说明
- [x] 返回值、空指针校验不适用（纯 Python pattern/lowering 注册）；meta/device/dtype 已做 guard
- [x] PR 标题使用 `fix` 类型标签
- [ ] 远端 CI 尚未执行；本地 `py_compile`、`git diff --check` 与 NPU 功能验证已通过

---

### Handoff

- **建议标题**：`fix(inductor): support dtype-accurate addcdiv re-fusion on NPU Triton experimental`
- **验证结论**：FP32/BF16 FMA 路径通过；FP16 value!=1 显式舍入路径 3/3 标量与 2/2 guard
  通过；value=1 按 bitwise-native 合同保持不命中且 NPU eager/compiled 位级通过
- **待办**：独立化产品补丁/分支与远端 CI
