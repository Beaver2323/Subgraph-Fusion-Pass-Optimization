# REF-unfuse-addmm-half-preserve-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-unfuse-bias-add-to-pointwise`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：bf16/fp16 产品保留 guard 验证；`product_gate_bypassed=false`

## 1. 原生阻断

参数化 `test_unfuse_bias_addmm_half_dtypes_{bfloat16,float16}` 受 `HAS_GPU` 门阻断。产品合同明确要求
half dtype 保持 addmm fused，不能为制造 ON path 临时关闭该 guard。

## 2. 最小适配

```python
# issues/REF-unfuse-addmm-half-preserve-native/npu_adapter.py:25-51
METHODS = ["..._bfloat16", "..._float16"]
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
```

显式运行两个原参数方法；dtype、`keep_addmm_fused_for_half_dtypes=true` 与 FileCheck 保持，产品 gate
未绕过。

## 3. 必要调用链

源码重建链：`two parameterized methods → torch.compile → half-dtype keep guard
→ fused addmm lowering → correctness/FileCheck`。

## 4. 结果与边界

bf16/fp16 两例均证明保留路径正确；它们是显式关闭范围的功能证据，不需要 ON 性能对照。单元正式
仍为 `EXPECTED_PRODUCT_DIVERGENCE`，详见[复现报告](复现报告.md)。
