# REF-pad-bmm-autocast-regression-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-bmm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：autocast 回归入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

`PadMMTest.test_no_autocast_in_pad_bmm_joint_graph_pass` 由 GPU 测试类生成。该 related regression
检查 dtype/梯度相关边界；只验证普通 forward 数值不能替代它。

## 2. 最小适配

```python
# issues/REF-pad-bmm-autocast-regression-native/npu_adapter.py:36-48
torch.npu.set_device("npu:0")
upstream.GPU_TYPE = "npu"
class Case(unittest.TestCase):
    test = upstream.PadMMTest.test_no_autocast_in_pad_bmm_joint_graph_pass
```

仅绕过 GPU harness 并预选 backend，原 autocast、dtype、forward/backward 断言不变；没有启用产品
pad-bmm。

## 3. 必要调用链

源码重建链：`Case.test → upstream regression body → AOTAutograd/joint graph → pad_bmm gate
→ forward/backward dtype assertions`。

## 4. 结果与边界

adapter 1/1 通过，回归合同在当前关闭路径上正确；单元正式 verdict 仍为
`EXPECTED_PRODUCT_DIVERGENCE`。运行时长、结果与日志哈希见[复现报告](复现报告.md)。
