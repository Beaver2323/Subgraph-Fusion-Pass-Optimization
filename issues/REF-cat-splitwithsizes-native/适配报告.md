# REF-cat-splitwithsizes-native NPU 最小适配报告

> 更新时间：2026-09-07 18:45 CST（UTC+08:00）
> 任务/单元：T-079 / `AU-post-grad-cat-splitwithsizes-replace`
> 后端：`triton_experimental`

## 1. 原生阻断

原模块 `__main__` 的 `HAS_GPU` 启动门不识别 NPU，导致返回 0、执行 0 tests。

## 2. 最小适配

```python
# issues/REF-cat-splitwithsizes-native/npu_adapter.py
runner = Path(__file__).parents[2] / "runners" / "t079_npu_case.py"
sys.argv = [str(runner), "--case-id", "REF-cat-splitwithsizes-native", *sys.argv[1:]]

# runners/t079_npu_case.py
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
```

`adapter_deviation`：只做设备注入、显式实例化与诊断插桩关闭。`preserved_contract`：正例和 cat
多用户、异维、异数量、异边界四个 guard 及其原始断言均保持。`product_gate_bypassed=false`。

## 3. 必要调用链

源码重建链：

```text
npu_adapter.py -> t079_npu_case.py -> test_cat_splitwithsizes -> common
  -> torch.compile -> post_grad.is_valid_cat_splitwithsizes
  -> post_grad.cat_splitwithsizes_replace -> original assertions
```

## 4. 结果与边界

1/1 tests PASS；正例与四个 guard 的原 counter/node 判据全部通过。独立目标级性能测试进一步证明
Event p50 改善 13.03%，因此保留启用；适配 PASS 本身未被当作性能结论。完整背景与工件见
[`复现报告.md`](复现报告.md)。
