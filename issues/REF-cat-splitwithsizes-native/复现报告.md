# REF-cat-splitwithsizes-native NPU 复现报告

> 更新时间：2026-09-07 18:45 CST（UTC+08:00）
> 状态：功能 PASS，性能 `PERF_IMPROVED`
> 后端：`triton_experimental`

## 1. 社区合同

社区入口：`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_cat_splitwithsizes`，冻结
PyTorch `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。同边界、同维且 cat 单用户时可以直接
返回原输入 tuple；cat 多用户、异维、异数量、异边界必须拒绝。

```python
# PyTorch: test/inductor/test_pattern_matcher.py:1033
cat = torch.ops.aten.cat.default([a, b, c], 1)
parts = torch.ops.aten.split_with_sizes.default(cat, [2, 3, 5], 1)
return [s**2 for s in parts]
```

## 2. 原生复现

原模块因 `HAS_GPU=false` 未执行 unittest，结果为 0 tests / `NO_TESTS`。

## 3. 最小适配与调用链

见 [`适配报告.md`](适配报告.md)。社区方法 → `common()` → `torch.compile` →
`is_valid_cat_splitwithsizes` → `cat_splitwithsizes_replace` → 原断言。

## 4. 动态结果

1/1 tests PASS。正例 counter 为 `[1,2]`，cat 多用户、异维、异数量、异边界四个负例均为
`[0,0]`，原数值断言全部通过。目标级 6 臂结果：NPU Event p50/p99 改善 13.03%/31.43%，host
p50/p99 改善 16.19%/67.35%，结论 `PERF_IMPROVED`，无需产品修复。

功能工件：`/home/z50063656/tmp/t079-npu-results/REF-cat-splitwithsizes-native/adapter-4ke2_hfj/`；性能工件：
`/home/z50063656/tmp/t079-performance-results/cat-split-performance-20260907T101524+0800/`。
