# REF-addmm-symbolic-scalar-negative-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-post-grad-addmm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：动态负例测试入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

`TestPatternMatcher.test_addmm_symbolic_scalar` 受 GPU-only suite 门阻断。合同要求 dynamic=True、
Python/Symbolic scalar bias 和两个 operand 顺序均保持 target counter=0。

## 2. 最小适配

```python
# issues/REF-addmm-symbolic-scalar-negative-native/npu_adapter.py:34-43
torch.npu.set_device("npu:0")
upstream.GPU_TYPE = "npu"
suite = unittest.TestSuite([
    upstream.TestPatternMatcher("test_addmm_symbolic_scalar")
])
```

只显式实例化原方法并选择 NPU/backend；dynamic、bias 表达、顺序、counter=0 和 tensor-aware
assertEqual 均保留。首轮普通 unittest equality 缺陷不计入产品结论，详见[复现报告](复现报告.md)。

## 3. 必要调用链

源码重建链：`adapter.main → upstream symbolic-scalar method → dynamic torch.compile
→ addmm negative guard → original graph → correctness/counter=0 assertions`。

## 4. 结果与边界

修正测试 harness 后负例通过，证明不应融合的 symbolic/Python scalar 保持不命中；单元整体仍因
正例安装态关闭而为 `EXPECTED_PRODUCT_DIVERGENCE`，本报告不把负例 PASS 扩大为正例支持。
