# REF-partial-reuse-positive-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-reuse-partial`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：三个原生正例显式实例化；`product_gate_bypassed=false`

## 1. 原生阻断

模块 `__main__` 仅在 `HAS_GPU=true` 时运行，而上游定义不包含 NPU，故退出 0 但 0 tests。必须完整
运行 case0/case1/case2 三个正例，不能选择性保留已通过者，详见[复现报告](复现报告.md)。

## 2. 最小适配

```python
# issues/REF-partial-reuse-positive-native/npu_adapter.py:62-83
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
suite = unittest.TestSuite([NpuCase(method) for method in METHODS])
```

适配只实例化原类/方法并旁路记录整数 counter；测试体、shape、threshold、断言和产品 gate 不改。

## 3. 必要调用链

源码重建链：`adapter.main → three upstream methods → torch.compile → partial reduction reuse handler
→ npu_triton_helpers → generated reduction → correctness/counter=1`。

## 4. 结果与边界

适配真实执行 3 tests 并发现 helper 缺口；条件修复验证完成，单元性能因 shape 不同而为
`PERF_MIXED`。修复代码和邻近回归见[根因分析](根因分析.md)及[修复验证报告](修复验证报告.md)。
