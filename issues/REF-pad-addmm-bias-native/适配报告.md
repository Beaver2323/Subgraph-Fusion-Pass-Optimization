# REF-pad-addmm-bias-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-addmm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：bias 广播测试入口适配；`product_gate_bypassed=false`

## 1. 原生阻断

社区 `PadMMTest.test_pad_addmm_2d_bias` 只由 GPU harness 运行。它覆盖六种 bias shape；只挑一种
可广播 shape 会缩窄合同。

## 2. 最小适配

```python
# issues/REF-pad-addmm-bias-native/npu_adapter.py:41-50
class Case(unittest.TestCase):
    @upstream.inductor_config.patch(
        force_shape_pad=True, max_autotune_gemm_backends="TRITON,ATEN"
    )
    def test_bias(self):
        compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

适配逐一保留 `(1,1)/(1,6)/(4,1)/(4,6)/(1,)/(6,)`、matmul shape 和断言，仅替换设备/入口，
没有解除产品 gate。

## 3. 必要调用链

源码重建链：`Case.test_bias → six upstream bias variants → torch.compile(addmm)
→ pad_addmm gate/NPU lowering → broadcast correctness`。

## 4. 结果与边界

六个逻辑 case 全部正确，padding 仍关闭；正式 verdict 为 `EXPECTED_PRODUCT_DIVERGENCE`。
运行摘要见[复现报告](复现报告.md)。
