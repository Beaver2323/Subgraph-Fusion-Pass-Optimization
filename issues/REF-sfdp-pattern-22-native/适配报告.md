# T-106 pattern 22 最小适配报告

> 更新时间：2026-09-14 21:28 CST（UTC+08:00）。状态：原生阻断已确认，适配已准备，设备结果待复核。

## 1. 原生阻断与适配原因

[native_runs](native_runs/) 保存本例冻结源码的直接执行日志。原命令选择 `SDPAPatternRewriterGpuTests.test_sdpa_rewriter_22_gpu`，实际报 `AttributeError: module '__main__' has no attribute 'SDPAPatternRewriterGpuTests'`。日志中的 `Ran 1 test` 指 unittest 的 `_FailedTest`，不是原测试体执行。GPU 精确编号归因已经取得；NPU 测试发现失败不能算产品关闭或功能通过。

## 2. 最小适配与未改动范围

```python
# runners/attention_community_npu.py: 导入冻结 test/inductor/test_fused_attention.py
with mock.patch.object(inductor_utils, 'HAS_CUDA_AND_TRITON', True), \\
     mock.patch.object(inductor_utils, 'GPU_TYPE', 'cuda'), \\
     mock.patch.object(common_cuda, 'PLATFORM_SUPPORTS_FUSED_ATTENTION', True):
    spec.loader.exec_module(community)
community.GPU_TYPE = 'npu'  # 原方法的全局GPU_TYPE工厂也必须落到真实NPU
class NpuCase(community.SDPAPatternRewriterGpuTests):
    device = 'npu'
# issues/REF-sfdp-pattern-22-native/npu_adapter.py
main(22)  # 仍调用原 partialmethod，不重写测试体
```

导入补丁只允许原 CUDA 测试类被定义，结束后还原；不伪装 CUDA 硬件，不改产品 gate。实际导入前选择 `triton_experimental`，设备执行验证 NPU 可用、源码 commit 及加载的后端。

CUDA 生成代码符号断言 `aten._scaled_dot_product` 映射为 AST 识别的实际 `torch.ops.npu.npu_fusion_attention[_v3].default` 调用。这项**断言适配**明确记录为 `body_or_assertions_modified=true`，不能称所有断言原封不动。数值容差、原输入、训练/推理和 checkpoint 分支不修改。dropout 原例若不比较输出/梯度，只计其实际结构断言，不补造精度 PASS。

## 3. 必要调用链（源码重建）

```text
npu_adapter.py -> attention_community_npu.main(22)
 -> 原 test_sdpa_rewriter_22_gpu partialmethod -> _check_common
 -> torch.compile -> joint graph pass -> _sfdp_pattern_22_... replacement
 -> NPU lowering/codegen -> NPU 设备 -> 原数值/计数及适配后的代码断言
```

只读观察器分别保存精确 replacement 前后 FX。整体图变化、邻接 pattern 命中或只产生 attention 算子，都不能代替编号 22 的精确归因。

## 4. 结果与边界

测试适配要求 `product_gate_bypassed=false`；当前仅准备好入口，实际运行后还须核验该字段与加载源码，不能当作已通过结果。

默认安装态先运行；若失败，先阅读生成代码、IR 和 FX 定位，不自动启用注册候选。本例自己的失败/成功才是裁决依据，pattern 1 候选成功不能外推。若使用隔离候选，工件单独放 `candidate_runs/`，不计安装态修复。

执行入口：

```bash
# 从 /home/z50063656/tmp，激活 Pass 后执行
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/run_prepared_npu_case.py \\
  --task T-106 --case REF-sfdp-pattern-22-native \\
  --npu 5 --adapter
```

功能工件归档在本 issue 的 `evidence/`；最新裁决见 `results/current/T-106/npu_stage_reviews.json`。性能必须另有同后端、精确目标 OFF/ON、数值正确及实际设备执行门禁，当前未签发。本报告允许后续追加实际结果，不覆盖原生失败历史。
