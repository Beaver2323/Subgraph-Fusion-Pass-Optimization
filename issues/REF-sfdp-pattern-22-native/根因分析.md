# Pattern 22 根因分析报告

> 更新时间：2026-09-15 11:15 CST（UTC+08:00）。归并既有实测结论，不新增或覆盖历史运行。

## 测试目的

冻结PyTorch `8e86e0a` 的 `_test_sdpa_rewriter_22` 验证带加法mask的
`softmax(QKᵀ + mask) @ V` 改写及返回K/V。batch 4/1 × mask是否转置共四组，
原数值合同需12次Tensor比较，不以总counter代替。

## 根因类型

`behavior-mismatch`（图模式生成代码的广播形状错误）。

## 判定依据

[原始复现报告与完整日志](adapter_runs/native-20260914T225851-pu1y93tc/复现报告.md)：
FAIL，退出码1；3772/4096元素超差，最大绝对差160767568。
[数值失败分析](数值失败分析.md)保留错误栈、三种子逐kernel对照与反事实实验。

```text
# test/inductor/test_fused_attention.py:1387、1411（冻结8e86e0a）
_test_sdpa_rewriter_22 -> _check_common -> torch.compile
 -> joint_graph _sfdp_pattern_22 -> SDPA -> NPU数学展开
 -> NPUTritonKernel._maybe_rewrite_select_lane_load
 -> output_code.py 行最大值kernel -> 原eager/compiled断言失败
```

首处分歧在行最大值，非QK矩阵乘。只改生成副本四处mask切片首维为1后，
三个种子8个kernel及最终输出误差归零；后续通用候选和安装态原例均通过。

## NPU 能力匹配评估

数值语义适用于NPU；CUDA kernel符号断言已单独审查并适配数学展开，不能宣称实现完全一致。
目标已经入图，实际是NPU bmm/safe_softmax，不是缺dispatch或CPU兜底。
错误切片尺寸在传入Triton之前已由torch_npu发射；同一Triton版本修正发射后通过，
因此本次修改定位torch_npu codegen，不归因为Triton缺语法或CANN缺算子。
独立产品工作树未发现 `disabled_testcases.json`；未新增跳过或修改产品disable。
项目禁止套用伪CUDA capability，未使用技能示例中的设备伪装。

## 修改文件清单

| 文件路径 | 改动类型 | 改动摘要 |
| --- | --- | --- |
| torch_npu/_inductor/triton_experimental/codegen/triton.py | 修改 | 两个select-load方法保留逐load广播外维并约束拓宽读取边界 |

## 关键代码变更

```python
# codegen/triton.py，_maybe_rewrite_select_lane_load，发射尺寸（节选）
# 修改前：复用输出tile尺寸，错误扩大共享mask的singleton维
"[" + ", ".join(sizes) + "]"
# 修改后：逐load实际外维，内维取1
"[" + ", ".join("1" if s == inner_slot else f"{full_var}.shape[{s}]"
                 for s in range(ndim)) + "]"
```

另在零offset且存储大小可静态证明时，给拓宽load合并原mask与地址上下界；
未知布局、非规范指针保持原load，strided Case B不改。边界保护与广播修复分别说明，
不把潜在越界风险冒充已证明的唯一数值根因。

## 修复理由

错误源于torch_npu生成器，不修改冻结PyTorch；不按pattern编号硬编码、不扩大数值容差。

## 代码差异

[实际窄补丁](select_load_shape_bounds.patch)包含33行新增、1行删除；
[完整前后源码与哈希](deployment-20260915-select-load/deployment.json)可追溯。
独立源码工作树 `fix/t106-select-load-20260915` 基于 `83cc45248`，
两个方法与候选逐字一致；安装态有既有修复，不能将其当成该分支独立wheel验证。

## 初步验证日志

隔离候选原例退出码0，12次Tensor/4次改写，完整命令与日志见
[候选运行](adapter_runs/native-20260914T235538-hylgfr0n/)。
后续无候选安装态原例和三个邻接退出码均0，21次Tensor/9次改写；
安装态22场景/23执行边界全部通过，完整命令、原件、源码与代码块已归并于
[部署与边界回归报告](部署与边界回归报告.md)。仅Python改动，无C++重编译。

## 硬红线自检结果

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| CPU兜底检测 | PASS | 产品实际diff无cpu/to(cpu)调用；修复只处理Triton代码发射 |
| disabled_testcases新增 | PASS | 独立工作树 `git diff -- '**/disabled_testcases.json'` 无输出 |
| 无无关变更 | PASS | `git diff --stat` 仅1文件，33增1删；两个方法外AST不变 |
| 根因-修复一致性 | PASS | 生成维度错误对应逐load形状修复，保留原数值断言 |

### Handoff

- 阶段结论：behavior-mismatch，已修复并初步验证通过。
- 下一步：verify，收尾性能门禁与交付文档。
- 工件：本报告、窄补丁、部署/原例/邻接/边界原件。
- 修改文件数：1，无C/C++变更，无级联缺失。
- 阻塞条件：功能修复无阻塞；性能尚未完成，不签收益结论。
