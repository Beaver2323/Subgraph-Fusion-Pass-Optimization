# REF-pad-mm-exclusion-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-076｜`AU-pad-mm-mm`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：负例入口与诊断适配；`product_gate_bypassed=false`

## 1. 原生阻断

社区 `PadMMTest.test_exclude_padding` 由 GPU harness 生成。早期 adapter 还暴露 NPU lowering choice
不足和 inactive benchmark mock 问题；这些失败均保留，不能只展示最终通过轮。

## 2. 最小适配

```python
# issues/REF-pad-mm-exclusion-native/npu_adapter.py:69-97
class NpuExclusion(unittest.TestCase):
    ...
    compiled = torch.compile(fn, options={"npu_backend": "triton_experimental"})
```

适配只提供 NPU 输入、ATEN 可用路径和 artifact 捕获；社区排除 shape、目标不命中条件与数值断言
未改变，也没有解除 `disable_pad_mm`。

## 3. 必要调用链

源码重建链：`NpuExclusion.test → torch.compile → pad_mm exclusion predicate → product gate/ATEN path
→ target-absent 与 correctness assertions`。

## 4. 结果与边界

最终负例 adapter 通过并保持目标结构不存在；单元 verdict 为 `EXPECTED_PRODUCT_DIVERGENCE`。
失败轮、最终轮和为什么移除无效 mock 见[复现报告](复现报告.md)。
