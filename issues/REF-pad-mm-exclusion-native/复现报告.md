# REF-pad-mm-exclusion-native NPU 复现报告

> 更新时间：2026-09-02 04:02 CST（UTC+08:00）
> 当前阶段：产品 gate baseline 完成
> 结果分类：`PASS`（adapter；`EXPECTED_DISABLED`）

## 基本信息

- 原始测试名：`test/inductor/test_pad_mm.py::PadMMTest.test_exclude_padding`
- 目标 acceptance unit：`AU-pad-mm-mm`
- 测试目的：验证 padding profitability/exclusion cache；NPU 产品基线下还需验证关闭 gate 后不产生 cache 项。

## 原生优先执行

- run root：`/home/z50063656/tmp/t076-npu-results/REF-pad-mm-exclusion-native/direct-20260902T040000+0800`。
- 退出码 0、无 unittest 统计；GPU-only main 未进入 test body，分类为 `NO_TESTS`。

## 最小适配与失败诊断

- 首轮 `adapter-20260902T040300+0800` 和去掉 inactive benchmark mock 后的
  `adapter-20260902T040800+0800` 均在 lowering 阶段报
  `AlgorithmSelectorCache.__call__` 不接受新参数 `best_config_future`。
- 已检查失败 debug 产物：FX readable/transformed 均包含原始 `aten.mm.default`，但失败发生在
  scheduler/codegen 前，因此没有 `output_code.py`；NPU `lowering_override_list.py` 明确覆盖
  `aten.mm`，`lowering_fallback_list.py` 未把普通 `aten.mm.default` 列为 fallback。
- 根因是 compile scope 首次加载时选择了不匹配的新旧 selector 接口，而非目标算子未入图。
  在 fresh process 中先选择 `TORCHINDUCTOR_NPU_BACKEND=triton_experimental` 并调用官方注册入口；
  检测到最终方法签名已兼容，`selector_signature_compat=false`，没有安装本地方法 shim。

## 最终结果

- run root：`.../adapter-20260902T041500+0800`；1 test、0 failure/error/skip，29.947 s。
- 保留原始两个图：`a @ b` 与 `(a + 1) @ b`，输入均为两张 `(61,61)` float32 张量。
- 两图 eager/compiled correctness 均通过；每轮 pad cache entries 都是 0。
- `product_gate_bypassed=false`、`disable_pad_mm=true`、`shape_padding=false`。
- 成功轮不展开读取 `output_code.py`。

关键 SHA256：adapter `c069201f762bd185f8e561e208085179cacfd554b3def8eb884fbdf804149a26`；
result `ea3788904e00e6b0be4176bb960c9e5f2546060c3925b308ceaae1943f9c4881`；
code1/code2 `5f6202d74bbee30b882b40108da88d2dd83685558e479f653b340529a803a952` /
`7ec87d02f391479d754cec0855d4a52f796992e7416ecdaa8edae5854b886791`；
最终日志 `d2c4010efa56635ce94fd073022ab80a35099ea9660a0a600df2fe6908206e05`。

## 判断

GPU reference 的 cache 正向合同有效；NPU 产品配置关闭 pad-mm，因而不产生 profitability/exclusion
cache 是 `EXPECTED_DISABLED`。两张原始图的 correctness 有效，不进入 repair queue。

---

### Handoff

- **阶段结论**：`PASS`。
- **下一步动作**：已并入 `AU-pad-mm-mm` 正式 comparison。
- **传递工件**：本报告、adapter、最终 run root、两次失败 debug 路径。
- **阻塞条件**：无。
