# REF-unfuse-addmm-leaf-native 最小适配报告

> 复核时间：2026-09-07 11:20 CST（UTC+08:00）｜T-078｜`AU-post-grad-unfuse-bias-add-to-pointwise`
> PyTorch：`8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`｜后端：`triton_experimental`
> 类型：same-shape leaf-bias 原方法实例化；`product_gate_bypassed=false`

## 1. 原生阻断

`test_unfuse_same_shape_leaf_bias_addmm` 原生入口因 `HAS_GPU` 为 0 tests。该 case 针对 leaf bias 与
同 shape 边界，不能并入 expanded 或 broadcast case。

## 2. 最小适配

```python
# issues/REF-unfuse-addmm-leaf-native/npu_adapter.py:40-46
upstream.GPU_TYPE = "npu"
class NpuCase(upstream.TestPatternMatcher):
    device_type = "npu"
result = runner.run(unittest.TestSuite([NpuCase(METHOD)]))
```

仅替换设备和入口；leaf 身份、shape、数值、alias/结构断言及产品 gate 不变。

## 3. 必要调用链

源码重建链：`original leaf method → torch.compile → addmm unfuse matcher → product guard
→ original/generated NPU path → correctness/structure assertions`。

## 4. 结果与边界

adapter 正确执行社区合同，最终 rewrite 在产品范围关闭，正式为 `EXPECTED_PRODUCT_DIVERGENCE`。
原生 0 tests、adapter run 和证据文件见[复现报告](复现报告.md)。
