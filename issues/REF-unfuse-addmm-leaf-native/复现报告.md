# REF-unfuse-addmm-leaf-native NPU 复现报告

> 更新时间：2026-09-06 08:05 CST（UTC+08:00）
> 阶段结论：`PASS`（same-shape Parameter leaf 正确拆分）
> NPU 后端：`triton_experimental`

## 测试合同

上游方法：
`test/inductor/test_pattern_matcher.py::TestPatternMatcher.test_unfuse_same_shape_leaf_bias_addmm`；
冻结 PyTorch 为 `8e86e0a23e3679c2bf3406cf0837fcb6297a5d9b`。

模型参数 bias 与 mm 输出同为 FP32 `[10,20]`，输入矩阵为 `[10,15] @ [15,20]`，
函数返回 `(mm + Parameter bias).relu()`。Parameter 是合法 leaf，即使 shape 不需要广播，
仍应允许 addmm unfuse：

- compiled 与 eager 数值一致；
- generated code 不含 `extern_kernels.addmm`。

GPU 紧凑 reference 中该 case 通过，FX before/after 签名不同。

## 原生入口与适配

原生入口受 `HAS_GPU` 启动门阻断，退出 0 但执行 0 tests，日志为
`direct-8c0mg3ch/`。最小适配仅设置 `GPU_TYPE=npu` 并显式构造原方法；未修改
Parameter、shape、数值或 FileCheck 判据。

## 结果

```text
Ran 1 test in 27.430s
OK
```

有效工件：
`/home/z50063656/tmp/t078-npu-results/REF-unfuse-addmm-leaf-native/adapter-c054tt0o/`。

transformed FX：

```python
# test/inductor/test_pattern_matcher.py:1603
mm = torch.ops.aten.mm.default(primals_1, primals_2)
add = torch.ops.aten.add.Tensor(mm, primals_3)
relu = torch.ops.aten.relu.default(add)
```

output code 调用 `extern_kernels.mm(...)`，bias add、ReLU 和 backward threshold
进入 Triton pointwise kernel，未出现 extern addmm。原 compiled/eager 断言通过，0 skip，
无 CPU fallback，无需源码修复。

执行命令：

```bash
cd /home/z50063656/tmp
source /home/z50063656/Pass/activate_pass.sh
export ASCEND_RT_VISIBLE_DEVICES=0
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/issues/REF-unfuse-addmm-leaf-native/run_native.py --adapter
```

---

### Handoff

- **阶段结论**：PASS
- **目标行为**：same-shape Parameter leaf 拆为 mm + pointwise bias/ReLU
- **下一步动作**：执行 expanded/stride-0 bias 正例
- **传递工件**：runner、adapter、FX/IR/output_code
- **阻塞条件**：无
