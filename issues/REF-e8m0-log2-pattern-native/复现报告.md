# T-096 E8M0 log2 编码：NPU 复现

更新时间：2026-09-11 15:27 CST（UTC+08:00）。状态：原生/适配已运行，安装态失败，隔离候选通过原合同但未部署。

## 测试目的

复用 `test/inductor/test_fp8.py::TestE8M0Log2PatternBitManip.test_pattern_fires_and_is_correct`，验证正 FP32 输入的 E8M0 uint8 编码，后续两例保留 1 ULP 边界的数学正确性 oracle。

## 环境

使用 Pass Python 3.11、冻结 PyTorch `8e86e0a`、NPU 910B2；必须在 tmp 中、导入前选择 `triton_experimental`。不使用 SM100 PTX 指令，不伪造 GPU capability。

## 原生入口与最小适配

先运行未修改社区测试；若设备 harness 跳过，单独记录最小设备适配。上游 CUDA registration guard 并非明确 NPU 产品 disable，但未完成能力评估前不启用性能 ON。

## 结果与下一步

三个原生入口均tests=1、skip=1，均不算PASS。随后按原方法逐例设备适配，安装态普通例数值通过但目标不改图，另外两个边界例数学编码失败。
隔离NPU位运算注册候选再次逐例执行，3/3原合同、精确目标前后FX及实际NPU kernel路径均通过；未修改安装包。
各例完整命令和日志保存在对应issue的native_runs、adapter_runs、candidate_runs；[根因分析](根因分析.md)给出数值、必要调用栈和前后编译产物入口。
安装态部署/边界评审未完成，性能未测，不能报告完整产品支持或收益。
