# T-106 pattern 24：SDPA数学展开的代码断言适配

> 更新时间：2026-09-14 23:04 CST（UTC+08:00）；原失败 t106-native-kfb_8jxu 保留，复验通过。

复验原件：[t106-native-uvnnjcrx](evidence/t106-native-uvnnjcrx/adapter/result.json)。
原方法1/1、零skip，1次本编号改写与1次FP32输出比较通过；不含训练梯度。
实际代码为两次bmm加NPU safe-softmax，而不是融合FA kernel。原数值容差保留，未改产品选择，
未把本原例FP32通过外推到FP16/BF16或性能派生输入；独立性能图和计时另行审核。

## 原合同与首个阻断

MBart/PLBart式将batch/head合并后执行BMM，加float mask，再softmax/BMM恢复四维；本原例FP32、单shape、只推理，预期一次输出比较。

```python
# torch/_inductor/fx_passes/fuse_attention.py:716，结构节选，省略前置permute。
q = query.view(bs * n_head, -1, head_size)
k = key.reshape(bs * n_head, -1, head_size)
scores = torch.bmm(q, k.transpose(1, 2)).view(bs, n_head, seq_len, -1) + attention_mask
# 接着softmax/BMM并恢复四维；replacement使用SDPA，scale=1。
```

GPU本编号已由具名观察器确认。NPU原安装态本编号inference改写一次，但失败在
`_check_common -> self.assertIn("aten._scaled_dot_product", source_code)` 的NPU符号适配，尚未进入数值比较。
[本例失败栈及生成调用](安装态基线复核.md)证明实际是两个`extern_kernels.bmm`和NPU safe-softmax等Triton kernel，
不是融合attention调用。

## 与pattern 14同类，但不是借用其结果

本例已独立复现并读取自己的FX/IR/output_code：[本例原件](evidence/t106-native-kfb_8jxu/adapter/)。
replacement保持float加法mask，NPU SDPA展开为数学图；不能把缺少FA符号等同于pattern未命中。
机制说明及CUDA/NPU调用对照见[pattern 14分析](../REF-sfdp-pattern-14-native/代码断言适配分析.md)。
不修改mask/dtype、产品guard或安装包，不宣称缺少融合kernel已经“修好”。

## 最小适配与判据

仅对已经逐例审查的14/21/22/24允许显式`--attention-math-codegen`；同时要求本编号真实改图、
两个实际NPU BMM、safe-softmax kernel及NPU设备选择。注释、字符串、邻接编号不能通过该检查。
这放宽的是CUDA专属代码符号表达方式，不修改原数值断言或容差。

```bash
# 激活Pass并从 /home/z50063656/tmp 启动，串行执行。
python /home/z50063656/Pass/Subgraph-Fusion-Pass-Optimization/scripts/run_prepared_npu_case.py \
  --task T-106 \
  --case REF-sfdp-pattern-24-native \
  --adapter \
  --attention-math-codegen \
  --npu 5 \
  --timeout 900
```

必须在复验后记录实际输出/容器叶子、精确改写次数和原方法结果，再做独立OFF/ON性能图。
旧原件不追写，新阶段索引更新前保存旧索引和报告。
